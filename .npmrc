# @figma packages (e.g. @figma/code-connect, used for Code Connect mappings)
# are not mirrored in the CodeArtifact upstream, so resolve that scope from
# public npm. All other packages continue to use the default registry.
@figma:registry=https://registry.npmjs.org/
