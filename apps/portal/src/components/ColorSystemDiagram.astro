---
// Color System Diagram - shows flow from Core Palette to Semantic Tokens to Themes
---

<div class="diagram not-content">
  <div class="card">
    <div class="card-content">
      <div class="palette-grid">
        <span style="background: #ef4444;"></span>
        <span style="background: #f97316;"></span>
        <span style="background: #eab308;"></span>
        <span style="background: #22c55e;"></span>
        <span style="background: #3b82f6;"></span>
        <span style="background: #8b5cf6;"></span>
        <span style="background: #06b6d4;"></span>
        <span style="background: #71717a;"></span>
        <span style="background: #ec4899;"></span>
      </div>
    </div>
    <div class="card-label">Core Palette</div>
  </div>
  
  <svg class="arrow" width="20" height="10" viewBox="0 0 20 10">
    <path d="M0 5 L14 5 M10 1 L16 5 L10 9" stroke="currentColor" stroke-width="1.2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>
  
  <div class="card semantic">
    <div class="card-content">
      <div class="list">
        <div class="row"><span class="dot" style="background: var(--sl-color-text);"></span>text</div>
        <div class="row"><span class="dot" style="background: var(--sl-color-bg); border: 1px solid var(--sl-color-gray-5);"></span>bg</div>
        <div class="row"><span class="dot" style="background: var(--sl-color-accent);"></span>accent</div>
      </div>
    </div>
    <div class="card-label">Semantic</div>
  </div>
  
  <svg class="arrow" width="20" height="10" viewBox="0 0 20 10">
    <path d="M0 5 L14 5 M10 1 L16 5 L10 9" stroke="currentColor" stroke-width="1.2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>
  
  <div class="card">
    <div class="card-content themes-content">
      <div class="themes-group">
        <div class="themes-row">
          <div class="theme-chip light">Light</div>
          <div class="theme-chip dark">Dark</div>
        </div>
        <div class="themes-more">+ other modes...</div>
      </div>
    </div>
    <div class="card-label">Themes</div>
  </div>
</div>

<style>
  .diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 16px 0;
  }
  
  .card {
    width: 105px;
    background: var(--sl-color-gray-7);
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 10px;
    overflow: hidden;
  }
  
  .card.semantic {
    background: color-mix(in srgb, var(--sl-color-accent) 5%, transparent);
    border-color: color-mix(in srgb, var(--sl-color-accent) 30%, transparent);
  }
  
  .card-content {
    height: 70px;
    padding: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .card-label {
    font-size: 8px;
    font-weight: 600;
    color: var(--sl-color-gray-3);
    text-transform: uppercase;
    letter-spacing: 0.4px;
    padding: 5px;
    background: var(--sl-color-gray-6);
    border-top: 1px solid var(--sl-color-gray-5);
    text-align: center;
  }
  
  .card.semantic .card-label {
    background: color-mix(in srgb, var(--sl-color-accent) 12%, transparent);
    border-color: color-mix(in srgb, var(--sl-color-accent) 20%, transparent);
  }
  
  .arrow {
    color: var(--sl-color-gray-4);
    flex-shrink: 0;
  }
  
  /* Core Palette Grid */
  .palette-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 4px;
  }
  
  .palette-grid span {
    width: 14px;
    height: 14px;
    border-radius: 3px;
  }
  
  /* List for Semantic */
  .list {
    display: flex;
    flex-direction: column;
    gap: 3px;
    width: 100%;
  }
  
  .row {
    display: flex;
    align-items: center;
    gap: 5px;
    font-size: 9px;
    font-family: ui-monospace, monospace;
    color: var(--sl-color-gray-2);
    line-height: 1;
  }
  
  /* Semantic dots */
  .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  
  /* Themes */
  .themes-content {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0;
    width: 100%;
    height: 100%;
  }
  
  .themes-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
  }
  
  .themes-row {
    display: flex;
    gap: 4px;
  }
  
  .theme-chip {
    padding: 2px 7px;
    border-radius: 3px;
    font-size: 9px;
    font-weight: 500;
    line-height: 1.2;
  }
  
  .theme-chip.light {
    background: #fff;
    color: #27272a;
    border: 1px solid #e4e4e7;
  }
  
  .theme-chip.dark {
    background: #18181b;
    color: #fafafa;
    border: 1px solid #3f3f46;
  }
  
  .themes-more {
    font-size: 8px;
    color: var(--sl-color-gray-3);
    margin-top: 1px;
  }
</style>
