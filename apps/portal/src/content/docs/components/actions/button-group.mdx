---
title: Button Group
description: ButtonGroup component
---

import { DocsPage } from "@/components/docs-page";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import PageTabsToc from "@/components/page-tabs-toc.astro";

<PageTabsToc />

<div id="page-level-tabs">

<Tabs>

<TabItem label="Overview">

The `ButtonGroup` component allows you to group multiple buttons together, either horizontally or vertically. It is useful for organizing related actions.
The button data is passed via the `buttonsProps` prop, which should contain an array of props for each individual button.

For a group of icon-only buttons, see the icon-only [Button Group](/components/actions/icon-button-group/).

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-center gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono">default</span>
    <ButtonGroup
      buttonsProps={[
        { children: 'Button', onClick: () => { console.log('Button 1') } },
        { children: 'Button', onClick: () => { console.log('Button 2') } },
        { children: 'Button', onClick: () => { console.log('Button 3') } }
      ]}
    />
  </div>
</div>`}
/>

## Usage

```typescript jsx
import { ButtonGroup } from '@harnessio/ui/components'

return (
  <ButtonGroup
    buttonsProps={[
      { children: 'Button', onClick: () => { console.log('Button 1') } },
      { children: 'Button', onClick: () => { console.log('Button 2') } },
      { children: 'Button', onClick: () => { console.log('Button 3') } }
    ]}
  />
)
```

## Layout Orientation

The `ButtonGroup` component accepts an `orientation` prop, which defines the layout direction: either `horizontal` or `vertical`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-md w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono pt-cn-xs">horizontal</span>
    <ButtonGroup
      buttonsProps={[
        { children: 'Button', onClick: () => { console.log('Button 1') } },
        { children: 'Button', onClick: () => { console.log('Button 2') } },
        { children: 'Button', onClick: () => { console.log('Button 3') } }
      ]}
    />
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono pt-cn-xs">vertical</span>
    <ButtonGroup
      orientation="vertical"
      buttonsProps={[
        { children: 'Button', onClick: () => { console.log('Button 1') } },
        { children: 'Button', onClick: () => { console.log('Button 2') } },
        { children: 'Button', onClick: () => { console.log('Button 3') } }
      ]}
    />
  </div>
</div>`}
/>

## Sizes

The `ButtonGroup` component accepts a `size` prop to control its overall dimensions. Supported values are: `default`, `sm`, and `xs`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-center gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono">md</span>
    <ButtonGroup
      buttonsProps={[
        { children: 'Button', onClick: () => { console.log('Button 1') } },
        { children: 'Button', onClick: () => { console.log('Button 2') } },
        { children: 'Button', onClick: () => { console.log('Button 3') } }
      ]}
    />
  </div>
  <div className="flex items-center gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono">sm</span>
    <ButtonGroup
      size="sm"
      buttonsProps={[
        { children: 'Button', onClick: () => { console.log('Button 1') } },
        { children: 'Button', onClick: () => { console.log('Button 2') } },
        { children: 'Button', onClick: () => { console.log('Button 3') } }
      ]}
    />
  </div>
  <div className="flex items-center gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono">xs</span>
    <ButtonGroup
      size="xs"
      buttonsProps={[
        { children: 'Button', onClick: () => { console.log('Button 1') } },
        { children: 'Button', onClick: () => { console.log('Button 2') } },
        { children: 'Button', onClick: () => { console.log('Button 3') } }
      ]}
    />
  </div>
</div>`}
/>

## With Dropdown

Within `buttonsProps`, you can provide a `dropdownProps` property to define the dropdown content for a specific button.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-center gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-32 shrink-0 font-mono">dropdown</span>
    <ButtonGroup
      buttonsProps={[
        {
          children: 'Button',
          dropdownProps: {
              content: (
                <>
                  <DropdownMenu.Item title="Item title" />
                  <DropdownMenu.Item title="With description" description="Item description" />
                  <DropdownMenu.Separator />
                  <DropdownMenu.Item title="With label" label="Label" />
                  <DropdownMenu.Item title="With checkmark" checkmark />
                  <DropdownMenu.Item title="With shortcut" shortcut="⇧⌘P" />
                </>
              )
          }
        },
        {
          children: 'Button',
          dropdownProps: {
              content: (
                <>
                  <DropdownMenu.Item title="Item title" />
                  <DropdownMenu.Item title="With description" description="Item description" />
                  <DropdownMenu.Separator />
                  <DropdownMenu.Item title="With label" label="Label" />
                  <DropdownMenu.Item title="With checkmark" checkmark />
                  <DropdownMenu.Item title="With shortcut" shortcut="⇧⌘P" />
                </>
              )
          }
        }
      ]}
    />
  </div>
</div>`}
/>

</TabItem>

<TabItem label="Properties">

{/* Visually hidden — anchors the section headings below in the TOC without showing a redundant title in the panel body (the tab already says "Properties"). Hidden via #page-level-tabs h2#properties in styles.css. */}

## Properties

Type `ButtonGroupButtonProps`

```typescript
type ButtonGroupTooltipProps = Pick<
  TooltipProps,
  "title" | "content" | "side" | "align"
>;

type BaseButtonProps = Omit<
  ButtonProps,
  "variant" | "size" | "theme" | "asChild" | "iconOnly" | "rounded" | "type"
>;

type DropdownPropsType = {
  content: ReactNode;
  contentProps?: typeof DropdownMenu.Content;
};

type ButtonWithTooltip = BaseButtonProps & {
  tooltipProps: ButtonGroupTooltipProps;
  dropdownProps?: undefined;
};

type ButtonWithDropdown = BaseButtonProps & {
  dropdownProps: DropdownPropsType;
  tooltipProps?: undefined;
};

type ButtonGroupButtonProps = ButtonWithTooltip | ButtonWithDropdown;
```

<DocsPage.PropsTable
  props={[
    {
      name: "orientation",
      description: "Whether to display stacked.",
      required: false,
      value: "'horizontal' | 'vertical'",
      defaultValue: "horizontal",
    },
    {
      name: "className",
      description: "Additional class names.",
      required: false,
      value: "string",
    },
    {
      name: "size",
      description: "The size of the buttons.",
      required: false,
      value: "'md' | 'sm' | 'xs'",
      defaultValue: "md",
    },
    {
      name: "buttonsProps",
      description:
        "Contains an array of button data, with support for either tooltipProps or dropdownProps for each button.",
      required: true,
      value: "ButtonGroupButtonProps[]",
    },
    {
      name: "iconOnly",
      description: "Optimizes the buttons styling for displaying only an icon.",
      required: false,
      value: "boolean",
    },
  ]}
/>

</TabItem>

</Tabs>

</div>
