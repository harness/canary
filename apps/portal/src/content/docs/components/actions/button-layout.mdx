---
title: Button Layout
description: ButtonLayout component
---

import { DocsPage } from "@/components/docs-page";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import PageTabsToc from "@/components/page-tabs-toc.astro";

<PageTabsToc />

<div id="page-level-tabs">

<Tabs>

<TabItem label="Overview">

The `ButtonLayout` component allows you to group multiple buttons together, either horizontally or vertically. It is useful for organizing related actions.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">default</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout>
        <Button variant="outline">Button outline</Button>
        <Button variant="secondary">Button secondary</Button>
        <Button>Button primary</Button>
      </ButtonLayout>
    </div>
  </div>
</div>`}
/>

## Usage

```typescript jsx
import { Button, ButtonLayout } from '@harnessio/ui/components'

return (
  <ButtonLayout horizontalAlign="start">
    <Button>Button primary</Button>
    <Button variant="secondary">Button secondary</Button>
    <Button variant="outline">Button outline</Button>
  </ButtonLayout>

  <ButtonLayout orientation="vertical">
    <Button>Button primary</Button>
    <Button variant="secondary">Button secondary</Button>
    <Button variant="outline">Button outline</Button>
  </ButtonLayout>

  <ButtonLayout.Root>
    <ButtonLayout.Primary>
      <Button>Button primary</Button>
    </ButtonLayout.Primary>
    <ButtonLayout.Secondary>
      <Button variant="secondary">Button secondary</Button>
    </ButtonLayout.Secondary>
  </ButtonLayout.Root>
)
```

## Anatomy

All parts of the `ButtonLayout` component can be imported and composed as required.

```typescript jsx
<ButtonLayout.Root>
  <ButtonLayout.Primary />
  <ButtonLayout.Secondary />
</ButtonLayout.Root>
```

## Layout Orientation

The `ButtonLayout` component accepts an `orientation` prop, which defines the layout direction: either `horizontal` or `vertical`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-md w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">horizontal</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout>
        <Button variant="outline">Button outline</Button>
        <Button variant="secondary">Button secondary</Button>
        <Button>Button primary</Button>
      </ButtonLayout>
    </div>
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">vertical</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout orientation="vertical">
        <Button>Button primary</Button>
        <Button variant="secondary">Button secondary</Button>
        <Button variant="outline">Button outline</Button>
      </ButtonLayout>
    </div>
  </div>
</div>`}
/>

## Horizontal Alignment

When `orientation` is `horizontal`, the `horizontalAlign` prop controls alignment. Use `start` to align buttons to the left, or `end` to align them to the right.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-md w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">end</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout>
        <Button variant="outline">Button outline</Button>
        <Button variant="secondary">Button secondary</Button>
        <Button>Button primary</Button>
      </ButtonLayout>
    </div>
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">start</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout horizontalAlign="start">
        <Button>Button primary</Button>
        <Button variant="secondary">Button secondary</Button>
        <Button variant="outline">Button outline</Button>
      </ButtonLayout>
    </div>
  </div>
</div>`}
/>

## Primary and secondary button groups

The component provides `ButtonLayout.Primary` and `ButtonLayout.Secondary` compositions for grouping buttons.

When `orientation` is set to `horizontal`:

- `Primary` aligns buttons to the **right**
- `Secondary` aligns buttons to the **left**

When `orientation` is set to `horizontal` and `horizontalAlign` is set to `start`:

- `Primary` aligns buttons to the **left**
- `Secondary` aligns buttons to the **right**

When `orientation` is set to `vertical`:

- `Primary` places buttons at the **top**
- `Secondary` places buttons at the **bottom**

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-md w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">horizontal</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout.Root>
        <ButtonLayout.Primary>
          <Button>Button primary</Button>
        </ButtonLayout.Primary>
        <ButtonLayout.Secondary>
          <Button variant="secondary">Button secondary</Button>
        </ButtonLayout.Secondary>
      </ButtonLayout.Root>
    </div>
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">vertical</span>
    <div className="flex-1 min-w-0">
      <ButtonLayout.Root orientation="vertical">
        <ButtonLayout.Primary>
          <Button>Button primary</Button>
        </ButtonLayout.Primary>
        <ButtonLayout.Secondary>
          <Button variant="secondary">Button secondary</Button>
        </ButtonLayout.Secondary>
      </ButtonLayout.Root>
    </div>
  </div>
</div>`}
/>

</TabItem>

<TabItem label="Properties">

{/* Visually hidden — anchors the section headings below in the TOC without showing a redundant title in the panel body (the tab already says "Properties"). Hidden via #page-level-tabs h2#properties in styles.css. */}

## Properties

### `Root`

The `Root` component serves as the main container for `Button`, `ButtonLayout.Primary` and `ButtonLayout.Secondary` components.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description:
        "Button, ButtonLayout.Primary and ButtonLayout.Secondary components.",
      required: true,
      defaultValue: "",
      value: "ReactNode",
    },
    {
      name: "orientation",
      description: "Whether to display stacked.",
      required: false,
      value: "'horizontal' | 'vertical'",
      defaultValue: "horizontal",
    },
    {
      name: "horizontalAlign",
      description:
        "When orientation is 'horizontal', align to the left when 'start' and right when 'end'.",
      required: false,
      value: "'start' | 'end'",
      defaultValue: "end",
    },
    {
      name: "className",
      description: "Additional class names.",
      required: false,
      value: "string",
    },
  ]}
/>

### `Primary`

`ButtonLayout.Primary` is placed on top when orientation is `vertical`

`ButtonLayout.Primary` appears on the right when orientation is `horizontal`

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Button components.",
      required: true,
      defaultValue: "",
      value: "ReactNode",
    },
    {
      name: "className",
      description: "Additional class names.",
      required: false,
      value: "string",
    },
  ]}
/>

### `Secondary`

`ButtonLayout.Secondary` is placed on bottom when orientation is `vertical`

`ButtonLayout.Secondary` appears on the left when orientation is `horizontal`

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Button components.",
      required: true,
      defaultValue: "",
      value: "ReactNode",
    },
    {
      name: "className",
      description: "Additional class names.",
      required: false,
      value: "string",
    },
  ]}
/>

</TabItem>

</Tabs>

</div>
