---
title: Avatar
description: Avatar component
beta: true
---

The `Avatar` component provides a way to represent an image with a fallback for when the image cannot be loaded. It supports displaying initials derived from the `name` prop or an Avatar Icon if no `name` or `src` is provided.

import { DocsPage } from "../../../components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-10">
    <div>
      <h6 className="font-semibold mb-2 text-center">Image (Default)</h6>
      <div className="flex items-center justify-center gap-6">
        <Avatar name="John Doe" src="https://github.com/shadcn.png" size="sm" />
        <Avatar name="John Doe" src="https://github.com/shadcn.png" />
        <Avatar name="John Doe" src="https://github.com/shadcn.png" size="lg" />
      </div>
    </div>
    <div>
      <h6 className="font-semibold mb-2 text-center">Image (Rounded)</h6>
      <div className="flex items-center justify-center gap-6">
        <Avatar name="John Doe" src="https://github.com/shadcn.png" size="sm" rounded />
        <Avatar name="John Doe" src="https://github.com/shadcn.png" rounded />
        <Avatar name="John Doe" src="https://github.com/shadcn.png" size="lg" rounded />
      </div>
    </div>
    <div>
      <h6 className="font-semibold mb-2 text-center">Initials (Default)</h6>
      <div className="flex items-center justify-center gap-6">
        <Avatar name="John Doe" size="sm" />
        <Avatar name="John Doe" />
        <Avatar name="John Doe" size="lg" />
      </div>
    </div>
    <div>
      <h6 className="font-semibold mb-2 text-center">Initials (Rounded)</h6>
      <div className="flex items-center justify-center gap-6">
        <Avatar name="John Doe" size="sm" rounded />
        <Avatar name="John Doe" rounded />
        <Avatar name="John Doe" size="lg" rounded />
      </div>
    </div>
    <div>
      <h6 className="font-semibold mb-2 text-center">Avatar Icon (Default)</h6>
      <div className="flex items-center justify-center gap-6">
        <Avatar size="sm" />
        <Avatar />
        <Avatar size="lg" />
      </div>
    </div>
    <div>
      <h6 className="font-semibold mb-2 text-center">Avatar Icon (Rounded)</h6>
      <div className="flex items-center justify-center gap-6">
        <Avatar size="sm" rounded />
        <Avatar rounded />
        <Avatar size="lg" rounded />
      </div>
    </div>
</div>`}
/>

## Usage

```typescript jsx
// Image (Default)
<Avatar name="John Doe" src="https://github.com/shadcn.png" />
<Avatar name="John Doe" src="https://github.com/shadcn.png" size="sm" />
<Avatar name="John Doe" src="https://github.com/shadcn.png" size="lg" />

// Image (Rounded)
<Avatar name="John Doe" src="https://github.com/shadcn.png" rounded />
<Avatar name="John Doe" src="https://github.com/shadcn.png" size="sm" rounded />
<Avatar name="John Doe" src="https://github.com/shadcn.png" size="lg" rounded />

// Initials (Default)
<Avatar name="John Doe" />
<Avatar name="John Doe" size="sm" />
<Avatar name="John Doe" size="lg" />

// Initials (Rounded)
<Avatar name="John Doe" rounded />
<Avatar name="John Doe" size="sm" rounded />
<Avatar name="John Doe" size="lg" rounded />

// Avatar Icon (Default)
<Avatar />
<Avatar size="sm" />
<Avatar size="lg" />

// Avatar Icon (Rounded)
<Avatar rounded />
<Avatar size="sm" rounded />
<Avatar size="lg" rounded />
```

## Sizes

The `Avatar` component supports three sizes: `sm`, `default`, and `lg`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex items-center gap-6">
    <Avatar name="John Doe" src="https://github.com/shadcn.png" size="sm" />
    <Avatar name="John Doe" src="https://github.com/shadcn.png" />
    <Avatar name="John Doe" src="https://github.com/shadcn.png" size="lg" />
</div>`}
/>

## Variants

The `Avatar` component supports two visual variants: `default` and `rounded`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex items-center gap-6">
    <Avatar name="John Doe" />
    <Avatar name="John Doe" rounded />
</div>`}
/>

## API Reference

```typescript jsx
<Avatar
  name="John Doe"                     // [OPTIONAL] Full name of the subject
  src="https://github.com/shadcn.png" // [OPTIONAL] Source URL of the image
  size="lg"                           // [OPTIONAL] Size of the avatar ('default' | 'sm' | 'lg')
  rounded                             // [OPTIONAL] Whether the avatar is circular
/>
```

### Props

The `Avatar` component has several props to control its appearance:

<DocsPage.PropsTable
  props={[
    {
      name: "name",
      description:
        "Full name of the subject of the avatar. Used to generate initials for the fallback and alt text. If not provided, an Avatar Icon will be rendered if no `src` is provided.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "src",
      description:
        "The source URL of the image. If not provided, initials or an Avatar Icon will be rendered.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "size",
      description:
        "The size of the avatar. Can be one of 'default', 'sm', or 'lg'.",
      required: false,
      value: "'default' | 'sm' | 'lg'",
      defaultValue: "'default'",
    },
    {
      name: "rounded",
      description: "Whether to display the avatar as a circle.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>
