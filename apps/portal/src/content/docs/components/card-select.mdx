---
title: CardSelect
description: A card-based selection interface for single and multiple selections with flexible layouts
beta: true
---

The `CardSelect` component provides an accessible and customizable card-based selection interface. It supports both single and multiple selection modes with flexible layouts (horizontal, vertical, or grid), making it suitable for forms, settings, and decision workflows.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`
  <div className="flex flex-col space-y-cn-2xl">
    <CardSelect.Root type="single" layout="horizontal" onValueChange={console.log}>
      <CardSelect.Item value="option-1">
        <CardSelect.Title>Option 1</CardSelect.Title>
      </CardSelect.Item>
      <CardSelect.Item value="option-2">
        <CardSelect.Title>Option 2</CardSelect.Title>
      </CardSelect.Item>
    </CardSelect.Root>

    <CardSelect.Root type="multiple" defaultValue={["one"]} onValueChange={console.log}>
      <CardSelect.Item value="one">
        <CardSelect.Title>Option A</CardSelect.Title>
      </CardSelect.Item>
      <CardSelect.Item value="two">
        <CardSelect.Title>Option B</CardSelect.Title>
      </CardSelect.Item>
    </CardSelect.Root>

  </div>`}
/>

## Usage

```typescript jsx
import { CardSelect } from "@harnessio/ui/components";

// ...

return (
  <CardSelect.Root type="single">
    <CardSelect.Item value="option-1" icon="git-branch">
      <CardSelect.Title>Option 1</CardSelect.Title>
      <CardSelect.Description>With an icon</CardSelect.Description>
    </CardSelect.Item>

    <CardSelect.Item value="option-2" logo="jira">
      <CardSelect.Title>Option 2</CardSelect.Title>
      <CardSelect.Description>With a logo</CardSelect.Description>
    </CardSelect.Item>
  </CardSelect.Root>
)
```

### Selection Modes

The `CardSelect` supports two selection types:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-cn-2xl">
  <div className="flex flex-col space-y-cn-xs">
    <Text variant="heading-subsection" as="h4">Single Selection</Text>
    <CardSelect.Root type="single" defaultValue="single-1">
      <CardSelect.Item value="single-1" logo="gitlab">
        <CardSelect.Title>Option 1</CardSelect.Title>
        <CardSelect.Description>Behaves like a radio group</CardSelect.Description>
      </CardSelect.Item>
      <CardSelect.Item value="single-2" logo="bitbucket">
        <CardSelect.Title>Option 2</CardSelect.Title>
        <CardSelect.Description>Behaves like a radio group</CardSelect.Description>
      </CardSelect.Item>
    </CardSelect.Root>
  </div>

  <div className="flex flex-col space-y-cn-xs">
    <Text variant="heading-subsection" as="h4">Multiple Selection</Text>
    <CardSelect.Root type="multiple" defaultValue={["multi-1", "multi-2"]}>
      <CardSelect.Item value="multi-1" icon="git-pull-request-draft">
        <CardSelect.Title>Option A</CardSelect.Title>
        <CardSelect.Description>Behaves like a checkbox</CardSelect.Description>
      </CardSelect.Item>
      <CardSelect.Item value="multi-2" icon="git-merge">
        <CardSelect.Title>Option B</CardSelect.Title>
        <CardSelect.Description>Behaves like a checkbox</CardSelect.Description>
      </CardSelect.Item>
    </CardSelect.Root>
  </div>
</div>`}
/>

### Layout

Cards can be arranged horizontally, vertically, or in a grid:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-cn-2xl">
    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Vertical Layout (Default)</Text>
      <CardSelect.Root type="single" layout="vertical">
        <CardSelect.Item value="v1">
          <CardSelect.Title>First Card</CardSelect.Title>
          <CardSelect.Description>Cards stacked vertically</CardSelect.Description>
        </CardSelect.Item>
        <CardSelect.Item value="v2">
          <CardSelect.Title>Second Card</CardSelect.Title>
          <CardSelect.Description>Below the first card</CardSelect.Description>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Horizontal Layout</Text>
      <CardSelect.Root type="single" layout="horizontal">
        <CardSelect.Item value="Text">
          <CardSelect.Title>First Card</CardSelect.Title>
          <CardSelect.Description>Cards arranged side by side</CardSelect.Description>
        </CardSelect.Item>
        <CardSelect.Item value="h2">
          <CardSelect.Title>Second Card</CardSelect.Title>
          <CardSelect.Description>In the same row</CardSelect.Description>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Grid Layout</Text>
      <CardSelect.Root type="single" layout="grid" cols={2} rows={2} gap="md">
        <CardSelect.Item value="grid-1">
          <CardSelect.Title>Grid Item 1</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="grid-2">
          <CardSelect.Title>Grid Item 2</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="grid-3">
          <CardSelect.Title>Grid Item 3</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="grid-4">
          <CardSelect.Title>Grid Item 4</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

  </div>`}
/>

### Gap

Control the gap between cards using the `gap` prop:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-cn-2xl">
    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">XS Gap</Text>
      <CardSelect.Root type="single" gap="xs" layout="horizontal">
        <CardSelect.Item value="xs-1">
          <CardSelect.Title>XS 1</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="xs-2">
          <CardSelect.Title>XS 2</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="xs-3">
          <CardSelect.Title>XS 3</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Small Gap</Text>
      <CardSelect.Root type="single" gap="sm" layout="horizontal">
        <CardSelect.Item value="small-1">
          <CardSelect.Title>Small 1</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="small-2">
          <CardSelect.Title>Small 2</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="small-3">
          <CardSelect.Title>Small 3</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Medium Gap (Default)</Text>
      <CardSelect.Root type="single" gap="md" layout="horizontal">
        <CardSelect.Item value="medium-1">
          <CardSelect.Title>Medium 1</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="medium-2">
          <CardSelect.Title>Medium 2</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="medium-3">
          <CardSelect.Title>Medium 3</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Large Gap</Text>
      <CardSelect.Root type="single" gap="lg" layout="horizontal">
        <CardSelect.Item value="large-1">
          <CardSelect.Title>Large 1</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="large-2">
          <CardSelect.Title>Large 2</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="large-3">
          <CardSelect.Title>Large 3</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

  </div>`}
/>

### Icons and Logos

Cards can display either an icon or a logo:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-cn-md">
    <CardSelect.Root type="single">
      <CardSelect.Item value="icon" icon="git-branch">
        <CardSelect.Title>With Icon</CardSelect.Title>
      </CardSelect.Item>
      <CardSelect.Item value="logo" logo="kubernetes">
        <CardSelect.Title>With Logo</CardSelect.Title>
      </CardSelect.Item>
    </CardSelect.Root>
  </div>`}
/>

### Disabled

Cards can be disabled either individually or as a group:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-cn-2xl">
    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Individual Cards Disabled</Text>
      <CardSelect.Root type="single">
        <CardSelect.Item value="enabled" icon="git-branch">
          <CardSelect.Title>Enabled Card</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="disabled" icon="git-branch" disabled>
          <CardSelect.Title>Disabled Card</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

    <div className="flex flex-col space-y-cn-xs">
      <Text variant="heading-subsection" as="h4">Entire Group Disabled</Text>
      <CardSelect.Root type="single" disabled>
        <CardSelect.Item value="one" icon="git-branch">
          <CardSelect.Title>First Option</CardSelect.Title>
        </CardSelect.Item>
        <CardSelect.Item value="two" icon="git-branch">
          <CardSelect.Title>Second Option</CardSelect.Title>
        </CardSelect.Item>
      </CardSelect.Root>
    </div>

  </div>`}
/>

## API Reference

### Root

The `CardSelect.Root` is the top-level wrapper that controls selection state.

```typescript jsx
<CardSelect.Root
  type="single"                     // Required: 'single' or 'multiple'
  value={selectedValue}             // [OPTIONAL] For controlled usage
  defaultValue={defaultValue}       // [OPTIONAL] For selecting item initially
  onValueChange={handleChange}      // [OPTIONAL] Selection change handler
  layout="horizontal"               // [OPTIONAL] Card layout: 'horizontal', 'vertical', or 'grid'
  gap="md"                          // [OPTIONAL] Space between cards
  disabled={false}                  // [OPTIONAL] Disable all selections
>
  {/* CardSelect.Item components */}
</CardSelect.Root>
```

<DocsPage.PropsTable
  props={[
    {
      name: "type",
      description: "Selection mode of the component.",
      required: true,
      value: "'single' | 'multiple'",
    },
    {
      name: "value",
      description: "Current selected value(s) for controlled usage.",
      required: false,
      value: "unknown | unknown[]",
      defaultValue: "undefined | []",
    },
    {
      name: "defaultValue",
      description: "Initial value(s) for uncontrolled usage.",
      required: false,
      value: "unknown | unknown[]",
      defaultValue: "undefined",
    },
    {
      name: "onValueChange",
      description: "Callback fired when selection changes.",
      required: false,
      value: "(val: unknown) => void | (val: unknown[]) => void",
      defaultValue: "undefined",
    },
    {
      name: "layout",
      description: "Layout of the cards.",
      required: false,
      value: "'horizontal' | 'vertical' | 'grid'",
      defaultValue: "'vertical'",
    },
    {
      name: "gap",
      description: "Space between cards.",
      required: false,
      value: "'xs' | 'sm' | 'md' | 'lg'",
      defaultValue: "'md'",
    },
    {
      name: "rows",
      description: "Number of rows for grid layout.",
      required: false,
      value: "number",
      defaultValue: "undefined",
    },
    {
      name: "cols",
      description: "Number of columns for grid layout.",
      required: false,
      value: "number",
      defaultValue: "undefined",
    },
    {
      name: "disabled",
      description: "When true, disables all selections.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>

### Item

Represents an individual selectable card option.

```typescript jsx
<CardSelect.Item
  value="unique-id"               // Required: Unique identifier
  icon="git-branch"                  // [OPTIONAL] Icon to display
  logo="gitlab"                  // [OPTIONAL] Logo instead of icon
  disabled={false}               // [OPTIONAL] Disable this item
>
  {/* CardSelect.Title and CardSelect.Description */}
</CardSelect.Item>
```

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description: "Unique identifier for the item.",
      required: true,
      value: "string",
    },
    {
      name: "icon",
      description: "Icon to display in the card.",
      required: false,
      value: "string",
    },
    {
      name: "logo",
      description: "Logo to display instead of an icon.",
      required: false,
      value: "string",
    },
    {
      name: "disabled",
      description: "When true, prevents this item from being selected.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>

### Title

Displays the main heading for a card item.

```typescript jsx
<CardSelect.Title>
  Option Title
</CardSelect.Title>
```

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Text content of the title.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Description

Provides additional details for a card item.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Text content of the description.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>
