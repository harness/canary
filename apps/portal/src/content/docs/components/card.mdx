---
title: Card
description: Card component
beta: true
---

The `Card` component provides a flexible UI element for displaying content within a card layout. It supports various configurations such as sizes, orientations, and the position of the image relative to the content.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-8">

    <Card.Root>
      <Card.Image src="/card-image.png" />
      <Card.Title>Complete Card</Card.Title>
      <Card.Content>A card showcasing all elements: image, title, and content.</Card.Content>
    </Card.Root>

    <Card.Root>
      <Card.Title>Title Only</Card.Title>
    </Card.Root>

    <Card.Root>
      <Card.Content>A card with just content text, without any title or image.</Card.Content>
    </Card.Root>

    <Card.Root>
      <Card.Title>Title and Content</Card.Title>
      <Card.Content>The most common card type with both a title and content.</Card.Content>
    </Card.Root>

  </div>`}
/>

## Usage

```typescript jsx
import { Card } from '@harnessio/ui'

// ...

return (
  <Card.Root orientation="vertical" position="start">
    <Card.Image src="/card-image.png" />
    <Card.Title>Card Title</Card.Title>
    <Card.Content>
      Card content goes here
    </Card.Content>
  </Card.Root>
)
```

## Sizes

The `Card` component supports three sizes: `sm`, `default`, and `md`. The size prop does not actually change the size of the card, but rather the padding and border radius.

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-4">
    <Card.Root size="sm">
      <Card.Title>Small Card</Card.Title>
      <Card.Content>Compact size</Card.Content>
    </Card.Root>
    <Card.Root>
      <Card.Title>Default Card</Card.Title>
      <Card.Content>Standard size</Card.Content>
    </Card.Root>
    <Card.Root size="md">
      <Card.Title>Medium Card</Card.Title>
      <Card.Content>Larger size</Card.Content>
    </Card.Root>
  </div>`}
/>

## Orientation

The `Card` component supports two orientations: `vertical` and `horizontal`. The orientation determines how the card's content is laid out.

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-4">
    <Card.Root orientation="vertical">
      <Card.Image src="/card-image.png" />
      <Card.Title>Vertical Layout</Card.Title>
      <Card.Content>Content stacks vertically below the image</Card.Content>
    </Card.Root>
    <Card.Root orientation="horizontal">
      <Card.Image src="/card-image.png" />
      <Card.Title>Horizontal Layout</Card.Title>
      <Card.Content>Content appears side-by-side with the image</Card.Content>
    </Card.Root>
  </div>`}
/>

## Image Position

The `position` prop controls where the image is placed within the card layout. It supports two values: `start` and `end`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-4">
    <Card.Root orientation="horizontal" position="start">
      <Card.Image src="/card-image.png" />
      <Card.Title>Image at Start</Card.Title>
    </Card.Root>
    <Card.Root orientation="horizontal" position="end">
      <Card.Title>Image at End</Card.Title>
      <Card.Image src="/card-image.png" />
    </Card.Root>
  </div>`}
/>

## States

The `Card` component supports `selected` and `disabled` states:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex gap-4">
    <Card.Root selected>
      <Card.Title>Selected Card</Card.Title>
      <Card.Content>Active state</Card.Content>
    </Card.Root>
    <Card.Root disabled>
      <Card.Title>Disabled Card</Card.Title>
      <Card.Content>Inactive state</Card.Content>
    </Card.Root>
  </div>`}
/>

## API Reference

The `Card` component has several props to control its appearance:

<DocsPage.PropsTable
  props={[
    {
      name: "orientation",
      description: "The orientation of the card.",
      required: false,
      value: "'vertical' | 'horizontal'",
      defaultValue: "'vertical'",
    },
    {
      name: "position",
      description:
        "Controls the placement of the image relative to the content. For vertical orientation, 'start' places the image above the content, and 'end' places it below. For horizontal orientation, 'start' places the image to the left of the content, and 'end' places it to the right.",
      required: false,
      value: "'start' | 'end'",
      defaultValue: "'start'",
    },
    {
      name: "size",
      description: "The size of the card.",
      required: false,
      value: "'sm' | 'default' | 'lg'",
      defaultValue: "'default'",
    },
    {
      name: "selected",
      description: "Whether the card is selected.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "disabled",
      description: "Whether the card is disabled.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>
