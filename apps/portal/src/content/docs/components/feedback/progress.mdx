---
title: Progress
description: Progress component
---

The `Progress` component provides a flexible UI element for displaying progress indicators.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<div className="w-[600px]">
    <Progress value={0.75} state="processing" />
</div>`}
/>

## Usage

```typescript jsx
import { Progress } from '@harnessio/ui/components'

//...

return (
  <Progress value={0.75} state="processing" />
)
```

### Variants

The `Progress` component supports the `variant` prop to indicate the type of progress being displayed. It accepts `default` and `indeterminate` values. This defines whether the progress is measurable or ongoing without a known completion percentage.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">default</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress value={0.75} state="processing" />
    </div>
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">indeterminate</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress variant="indeterminate" />
    </div>
  </div>
</div>`}
/>

### States

The `Progress` component supports the `state` prop to reflect the current status of the operation. It accepts `completed`, `paused`, `failed`, `processing`, and `default` values. This controls the visual feedback based on the progress state.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">completed</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={1}
        subtitle="Completed"
        label="Analytics_Report_March.csv"
        state="completed"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">paused</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.5}
        label="Analytics_Report_March.csv"
        subtitle="Paused"
        state="paused"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">failed</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.5}
        label="Analytics_Report_March.csv"
        subtitle="Failed"
        description="Upload process failed due to connection issues."
        state="failed"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">processing</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.5}
        label="Analytics_Report_March.csv"
        subtitle="5 min left"
        state="processing"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">default</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.5}
        label="Analytics_Report_March.csv"
        subtitle="Subtitle"
        description="This is a description text"
      />
    </div>
  </div>
</div>`}
/>

### Colors

The `Progress` component supports a `color` prop that accepts a closed enum of palette themes — `gray`, `blue`, `brown`, `cyan`, `green`, `forest-green`, `indigo`, `lime`, `mint`, `orange`, `pink`, `purple`, `red`, `violet`, and `yellow` — matching the `Tag` theme set. The bar fill resolves to the corresponding design-system token (e.g. `--cn-set-blue-primary-bg`).

`color` and `state` are mutually exclusive — pass one or the other, never both. When `color` is set, the bar uses the chosen theme color, while the status icon falls back to the neutral default; use `hideIcon` if you want to remove the icon entirely. `color` works with both the `default` and `indeterminate` variants.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">blue</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.6}
        color="blue"
        label="Analytics_Report_March.csv"
        subtitle="60%"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">purple</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.4}
        color="purple"
        label="Analytics_Report_March.csv"
        subtitle="40%"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">forest-green</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        value={0.8}
        color="forest-green"
        label="Analytics_Report_March.csv"
        subtitle="80%"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">indeterminate + cyan</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        variant="indeterminate"
        color="cyan"
        label="Loading"
      />
    </div>
  </div>
</div>`}
/>

### Sizes

The `Progress` component supports the `size` prop to adjust the height of the progress bar. It accepts `sm`, `md`, and `lg` values. This allows customization of the component’s visual size to fit different UI contexts.

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-cn-sm w-full">
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">small</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        size="sm"
        value={0.5}
        label="Title"
        subtitle="Subtitle"
        description="This is a description text"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">medium</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        size="md"
        value={0.5}
        label="Title"
        subtitle="Subtitle"
        description="This is a description text"
      />
    </div>
  </div>
  <div className="flex items-start gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono pt-cn-xs">large</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress
        size="lg"
        value={0.5}
        label="Title"
        subtitle="Subtitle"
        description="This is a description text"
      />
    </div>
  </div>
  <div className="flex items-center gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono">sm · processing</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress size="sm" value={0.75} state="processing" />
    </div>
  </div>
  <div className="flex items-center gap-cn-md border-b border-cn-borders-3 pb-cn-sm">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono">md · processing</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress size="md" value={0.75} state="processing" />
    </div>
  </div>
  <div className="flex items-center gap-cn-md">
    <span className="text-cn-size-2 text-cn-3 w-40 shrink-0 font-mono">lg · processing</span>
    <div className="flex-1 min-w-0 max-w-[600px]">
      <Progress size="lg" value={0.75} state="processing" />
    </div>
  </div>
</div>`}
/>

## API Reference

<DocsPage.PropsTable
  props={[
    {
      name: "variant",
      description:
        "Type of progress. Use 'default' for determinate progress or 'indeterminate' for loading state.",
      required: false,
      defaultValue: "'default'",
      value: "'default' | 'indeterminate'",
    },
    {
      name: "value",
      description:
        "Progress value from 0 to 1. Required when variant is 'default'.",
      required: false,
      value: "number",
    },
    {
      name: "state",
      description:
        "Visual state of the progress. Affects bar color, status icon, and description/subtitle colors. Mutually exclusive with 'color' — pass one or the other.",
      required: false,
      defaultValue: "'default'",
      value: "'default' | 'processing' | 'completed' | 'paused' | 'failed'",
    },
    {
      name: "color",
      description:
        "Theme color for the bar fill, drawn from the same palette as Tag (with 'green', 'red', 'yellow' resolving to success/danger/warning tokens). Mutually exclusive with 'state'.",
      required: false,
      value:
        "'gray' | 'blue' | 'brown' | 'cyan' | 'green' | 'forest-green' | 'indigo' | 'lime' | 'mint' | 'orange' | 'pink' | 'purple' | 'red' | 'violet' | 'yellow'",
    },
    {
      name: "size",
      description: "Size of the progress component.",
      required: false,
      defaultValue: "'md'",
      value: "'sm' | 'md' | 'lg'",
    },
    {
      name: "label",
      description: "Primary label displayed above the progress bar.",
      required: false,
      value: "string",
    },
    {
      name: "description",
      description: "Additional description shown below the progress bar.",
      required: false,
      value: "string",
    },
    {
      name: "subtitle",
      description: "Secondary subtitle shown below the description.",
      required: false,
      value: "string",
    },
    {
      name: "hidePercentage",
      description: "If true, hides the progress percentage next to the label.",
      required: false,
      defaultValue: "false",
      value: "boolean",
    },
    {
      name: "hideIcon",
      description: "If true, hides the status icon depending on the state.",
      required: false,
      defaultValue: "false",
      value: "boolean",
    },
    {
      name: "id",
      description:
        "Custom ID for the progress element. Auto-generated if not provided.",
      required: false,
      value: "string",
    },
    {
      name: "className",
      description: "Additional class names to apply to the root container.",
      required: false,
      value: "string",
    },
  ]}
/>
