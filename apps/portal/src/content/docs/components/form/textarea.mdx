---
title: Textarea
description: Textarea component
beta: true
---

The `Textarea` component is used to create a multi-line text input field.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="1"
      name="default"
      label="Default"
      placeholder="Placeholder"
      caption="Caption text"
      rows={4}
    />
  </Layout.Vertical>
  `}
/>

## Usage

```typescript jsx
import { Textarea } from '@harnessio/ui/components'

//...

return (
  <Textarea
    label="Textarea label"
    placeholder="Placeholder"
    name="textarea-name"
    onChange={handleChange}
  />
)
```

## Disable

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="disabled"
      name="disabled"
      label="Disabled"
      disabled
      placeholder="Placeholder"
      rows={4}
    />
  </Layout.Vertical>
`}
/>

## Resizable

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="2"
      name="resizable"
      label="Resizable"
      placeholder="Placeholder"
      resizable
      rows={4}
    />
  </Layout.Vertical>
`}
/>

## Auto Resize

The `Textarea` component supports automatic height adjustment based on content with the `autoResize` prop. When enabled, the textarea will automatically grow and shrink to fit its content, eliminating the need for manual scrolling.

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="auto-resize"
      name="auto-resize"
      label="Auto Resize"
      placeholder="Start typing and watch the textarea grow automatically..."
      autoResize
    />
  </Layout.Vertical>
`}
/>

**Key Features:**

- **Modern CSS Support**: Uses native `field-sizing: content` when supported by the browser
- **Known Firefox issue**: The `autoResize` prop may not work as expected in Firefox due to its support of `field-sizing: content`.
- **Max Height Constraint**: Respects maximum height limits to prevent excessive growth

## Theme

The `Textarea` component accepts a `theme` prop, which defines the visual state of the input: either `warning`, or `danger`.

When an error occurs, set `theme="danger"` and pass the error message to the `error` prop.

For warnings, set `theme="warning"` and provide the warning message via the `warning` prop.

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="error"
      name="error"
      label="This component in error state"
      placeholder="Placeholder"
      theme="danger"
      error="Error message"
      optional
      rows={4}
    />
    <Textarea
      id="warning"
      name="warning"
      label="This component in warning state"
      placeholder="Placeholder"
      theme="warning"
      warning="Warning message"
      optional
      rows={4}
    />
  </Layout.Vertical>
`}
/>

## Character Limit

The `Textarea` component supports a `maxCharacters` prop, which defines the maximum number of characters a user can enter.

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="max-characters"
      name="max-characters"
      label="Character Limit"
      placeholder="Placeholder"
      maxCharacters={200}
      rows={4}
    />
  </Layout.Vertical>
`}
/>

## Sizes

The `Textarea` component supports the following sizes: `sm` and `md`. The default size is `md`.

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="1"
      name="md"
      label="Size md"
      placeholder="Placeholder"
      rows={4}
    />
    <Textarea
      id="2"
      name="sm"
      label="Size sm"
      placeholder="Placeholder"
      rows={4}
      size="sm"
    />
  </Layout.Vertical>
`}
/>

## With Tooltip

`tooltipContent` is used to display the content of the tooltip. `tooltipProps` is used to pass additional props to the tooltip.

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="with-tooltip"
      name="with-tooltip"
      label="With default Tooltip"
      placeholder="Placeholder"
      tooltipContent="Tooltip content"
      rows={4}
    />
    <Textarea
      id="with-tooltip-custom-icon"
      name="with-tooltip-custom-icon"
      label="With Tooltip and custom icon"
      placeholder="Placeholder"
      tooltipContent="Tooltip content with custom icon"
      tooltipProps={{
        disabled: false,
        iconProps: {
          name: 'warning-triangle',
          color: 'warning'
        },
      }}
      rows={4}
    />
  </Layout.Vertical>
`}
/>

## Orientation

The `Textarea` component accepts an optional `orientation` prop, which defines the layout direction of the textarea and its label.
Available options are `horizontal` and `vertical` (default).

<DocsPage.ComponentExample
  client:only
  code={`
  <Layout.Vertical gap="xl" className="min-w-[400px]">
    <Textarea
      id="vertical"
      name="vertical"
      label="Vertical"
      placeholder="Placeholder"
      rows={4}
      caption="Caption text"
      maxCharacters={200}
    />
    <Textarea
      id="horizontal"
      name="horizontal"
      label="Horizontal"
      placeholder="Placeholder"
      rows={4}
      orientation="horizontal"
      caption="Caption text"
      maxCharacters={200}
    />
  </Layout.Vertical>
`}
/>

## API Reference

The `Textarea` can be used either controlled or uncontrolled.

When controlled, the `Textarea` will use the `value` prop as its value and call the `onChange` callback whenever the value changes.

When uncontrolled, the `Textarea` will use the `defaultValue` prop as its initial value and will call `onChange` when its value changes.

The `Textarea` also supports all attributes of the `textarea` HTML element.

```typescript
interface IconWithTooltipProps extends Omit<TooltipProps, "children"> {
  className?: string;
  /**
   * If disabled, the tooltip will not be shown
   */
  disabled?: boolean;
  iconProps?: IconPropsV2;
}
```

<DocsPage.PropsTable
  props={[
    {
      name: "wrapperClassName",
      description: "Additional CSS classes for the wrapper element",
      required: false,
      defaultValue: "",
      value: "string",
    },
    {
      name: "className",
      description: "Additional CSS classes for the textarea element",
      required: false,
      defaultValue: "",
      value: "string",
    },
    {
      name: "name",
      description: "Name of the textarea.",
      required: false,
      value: "string",
    },
    {
      name: "id",
      description: "Id of the textarea.",
      required: false,
      value: "string",
    },
    {
      name: "value",
      description: "Value of the textarea.",
      required: false,
      value: "string",
    },
    {
      name: "onChange",
      description: "Callback to call when the value changes.",
      required: false,
      value: "(value: string) => void",
    },
    {
      name: "label",
      description: "Label for the textarea.",
      required: false,
      value: "string",
    },
    {
      name: "caption",
      description: "Caption to describe the textarea.",
      required: false,
      value: "string",
    },
    {
      name: "error",
      description: "Error message to display below.",
      required: false,
      value: "string",
    },
    {
      name: "warning",
      description: "Warning message to display (will use warning theme)",
      required: false,
      defaultValue: "",
      value: "string",
    },
    {
      name: "optional",
      description: "Indicate if the textarea is optional.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "autoResize",
      description:
        "Indicate if the textarea should automatically adjust its height based on content.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "resizable",
      description: "Indicate if the textarea should be resizable.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "disabled",
      description: "Indicate if the textarea is disabled.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "size",
      description: "Size of the textarea.",
      required: false,
      defaultValue: "'md'",
      value: "'sm' | 'md'",
    },
    {
      name: "maxCharacters",
      description: "Defines the maximum number of characters a user can enter.",
      required: false,
      value: "number",
    },
    {
      name: "tooltipContent",
      description: "Content to display in the label tooltip.",
      required: false,
      value: "Exclude<ReactNode, boolean | null | undefined>",
    },
    {
      name: "tooltipProps",
      description: "Additional props for configuring the tooltip.",
      required: false,
      value: "IconWithTooltipProps",
    },
    {
      name: "labelSuffix",
      description:
        "Customized with a suffix element that appears on the right side of the label.",
      required: false,
      value: "ReactNode",
    },
    {
      name: "orientation",
      description: "Defines the layout direction of the input and its label.",
      required: false,
      defaultValue: "'vertical'",
      value: "'vertical' | 'horizontal'",
    },
  ]}
/>
