---
title: Label
description: Label component
beta: true
---

The `Label` component is a customizable label for form elements. It includes an optional required indicator and follows consistent styling across the design system.

import { DocsPage } from "../../../components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`
    <div className="flex flex-col gap-4">
      <Label htmlFor="name">Name</Label>
      <input id="name" type="text" />
    </div>
`}
/>

## Usage

```typescript jsx
import { Label } from '@harnessio/ui/components'

// ...

return (
  <>
    <Label htmlFor="username">Username</Label>
    <input id="username" type="text" />
  </>
)
```

## Optional

<DocsPage.ComponentExample
  client:only
  code={`
    <div className="flex flex-col gap-4">
      <Label htmlFor="email" optional>Email</Label>
      <input id="email" type="email" />
    </div>
  `}
/>

## With informer

The `Label` component supports displaying a tooltip with additional information.
Use the `informerContent` prop to provide the tooltip content, and pass additional configuration via the `informerProps` prop.

<DocsPage.ComponentExample
  client:only
  code={`
    <div className="flex flex-col gap-4">
      <Label
        htmlFor="2"
        informerContent="Content here"
      >
        Description
      </Label>
      <input id="2" type="text" />
    </div>
  `}
/>

## Suffix

<DocsPage.ComponentExample
  client:only
  code={`
    <div className="flex flex-col gap-4">
      <Label
        htmlFor="2"
        informerContent="Content here"
        suffix={
          <Button size="xs" iconOnly>
            <IconV2 name="plus" skipSize />
          </Button>
        }
      >
        Description
      </Label>
      <input id="2" type="text" />
    </div>
  `}
/>

## API Reference

```typescript
interface InformerProps extends Omit<TooltipProps, "children"> {
  className?: string;
  /**
   * If disabled, the tooltip will not be shown
   */
  disabled?: boolean;
  iconProps?: IconPropsV2;
}
```

<DocsPage.PropsTable
  props={[
    {
      name: "htmlFor",
      description: "The id of the form control this label is associated with.",
      required: false,
      value: "string",
    },
    {
      name: "optional",
      description: "Indicates if the associated form field is optional.",
      defaultValue: "false",
      required: false,
      value: "boolean",
    },
    {
      name: "className",
      description: "Additional CSS classes to apply to the label.",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "The content of the label.",
      required: true,
      value: "ReactNode",
    },
    {
      name: "informerContent",
      description: "Content to display in the informer tooltip.",
      required: false,
      value: "Exclude<ReactNode, boolean | null | undefined>",
    },
    {
      name: "informerProps",
      description: "Additional props for configuring the informer tooltip.",
      required: false,
      value: "InformerProps",
    },
    {
      name: "suffix",
      description:
        "Customized with a suffix element that appears on the right side of the label.",
      required: false,
      value: "ReactNode",
    },
  ]}
/>
