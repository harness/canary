---
title: Logo
description: Logo component
beta: true
---

import { DocsPage } from "../../../components/docs-page";
import { LogoV2, LogoNameMapV2 } from "@harnessio/ui/components";
import CompactLogoV2Grid from "../../../components/CompactLogoV2Grid.astro";

## Usage

```typescript jsx
<LogoV2 name="kubernetes" size={48} />
```

## Logos

The following logos are available:

<CompactLogoV2Grid />

## API Reference

```typescript jsx
<LogoV2
  name="logo-name"     // name of the logo
  size={24}            // [OPTIONAL] size of the logo in pixels
  className="my-class" // [OPTIONAL] custom class name
  original             // [OPTIONAL] if false, the logo will use the current text color,
                       // otherwise will use its brand-specific color
/>
```

<DocsPage.PropsTable
  props={[
    {
      name: "name",
      description: "Name of the logo",
      required: true,
      value: "string",
    },
    {
      name: "size",
      description: "Size of the logo in pixels",
      required: false,
      value: "number",
      defaultValue: "24",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "original",
      description:
        "If true, the logo will use its brand-specific color, otherwise will use `currentColor`",
      required: false,
      value: "boolean",
      defaultValue: "true",
    },
  ]}
/>
