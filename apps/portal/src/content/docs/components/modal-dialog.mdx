---
title: ModalDialog
description: A dialog overlay that appears above the page content
beta: true
---

The `ModalDialog` component provides an accessible and customizable dialog overlay for displaying important content or requesting user input. It supports different sizes, themes, and can include an icon or logo in the header.

import { DocsPage } from "@/components/docs-page";
import { Aside } from "@astrojs/starlight/components";

<DocsPage.ComponentExample
  client:only
  code={`<ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Open Dialog</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content>
        <ModalDialog.Header>
          <ModalDialog.Title>Generate Clone Credential</ModalDialog.Title>
        </ModalDialog.Header>
        <ModalDialog.Body>
          <div className="flex flex-col gap-y-7">
            <TextInput
              className="py-px truncate"
              id="identifier"
              label="Name"
              value="code_token_yuiq6"
              readOnly
              suffix={<CopyButton buttonVariant="transparent" iconSize={14} name="code_token_yuiq6" />}
            />

            <TextInput
              className="py-px"
              id="lifetime"
              label="Expiration"
              value="1 year"
              readOnly
            />

            <TextInput
              className="truncate py-px"
              id="token"
              label="Token"
              value="bGciOiJSUzI1NiIsImtpZCI6IjZCQ0Q.yMzYyRTdFODQyODcyNTlCREYyMzY3RjUxM0I.2IiwidHlwIjoiYXQrand0In0.eyJuYmYiOjE2OTc0NjY2Mzk1OTIsImV4cCI6MTY5NzQ2NzI0MDAwMCwiaXNzIjoiaHR0cHM6Ly9hcHAuaGFybmVzcy5pbyJ9"
              readOnly
              suffix={<CopyButton buttonVariant="transparent" iconSize={14} name="bGciOiJSUzI1NiIsImtpZCI6IjZCQ0QyMzYyRTdFODQyODcyNTlCREYyMzY3RjUxM0I2IiwidHlwIjoiYXQrand0In0.eyJuYmYiOjE2OTc0NjY2Mzk1OTIsImV4cCI6MTY5NzQ2NzI0MDAwMCwiaXNzIjoiaHR0cHM6Ly9hcHAuaGFybmVzcy5pbyJ9" />}
            />

            <span>Token generated</span>
          </div>
        </ModalDialog.Body>
        <ModalDialog.Footer>
          <ButtonLayout>
            <ModalDialog.Close>Close</ModalDialog.Close>
            <Button type="button">
              Manage API Token
            </Button>
          </ButtonLayout>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>`}

/>

## Usage

```typescript jsx
import { ModalDialog } from "@harnessio/ui/components";

// ...

return (
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Open Dialog</Button>
    </ModalDialog.Trigger>

    <ModalDialog.Content size="md">
      <ModalDialog.Header icon="circle-plus">
        <ModalDialog.Title>Dialog Title</ModalDialog.Title>
        <ModalDialog.Description>Optional description text</ModalDialog.Description>
      </ModalDialog.Header>

      <ModalDialog.Body>
        Dialog content goes here
      </ModalDialog.Body>

      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Confirm</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
)
```

### Sizes

The `ModalDialog` component supports three sizes: `sm`, `md`, and `lg`:

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-4">
  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Small (Default)</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Small Dialog</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content size="sm">
        <ModalDialog.Header>
          <ModalDialog.Title>Small Dialog</ModalDialog.Title>
        </ModalDialog.Header>
        <ModalDialog.Description>Compact size for simple dialogs</ModalDialog.Description>
        <ModalDialog.Footer>
          <ModalDialog.Close>Close</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>

<div className="space-y-2">
  <h3 className="text-lg font-semibold">Medium</h3>
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Medium Dialog</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content size="md">
      <ModalDialog.Header>
        <ModalDialog.Title>Medium Dialog</ModalDialog.Title>
      </ModalDialog.Header>
      <ModalDialog.Description>
        Standard size for most use cases
      </ModalDialog.Description>
      <ModalDialog.Footer>
        <ModalDialog.Close>Close</ModalDialog.Close>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>

  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Large</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Large Dialog</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content size="lg">
        <ModalDialog.Header>
          <ModalDialog.Title>Large Dialog</ModalDialog.Title>
        </ModalDialog.Header>
        <ModalDialog.Description>Maximum width for complex content</ModalDialog.Description>
        <ModalDialog.Footer>
          <ModalDialog.Close>Close</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>
</div>`}
/>

### Themes

Headers can have themes `default`, `warning` and `danger` with matching icons:

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-4">
  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Default Theme</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Default</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content>
        <ModalDialog.Header icon="info-circle">
          <ModalDialog.Title>Information</ModalDialog.Title>
          <ModalDialog.Description>Default theme with info icon</ModalDialog.Description>
        </ModalDialog.Header>
        <ModalDialog.Footer>
          <ModalDialog.Close>Close</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>
  
  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Warning Theme</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Warning</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content>
        <ModalDialog.Header icon="triangle-warning" theme="warning">
          <ModalDialog.Title>Warning</ModalDialog.Title>
          <ModalDialog.Description>
            Warning theme with caution icon
          </ModalDialog.Description>
        </ModalDialog.Header>
        <ModalDialog.Footer>
          <ModalDialog.Close>Cancel</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>
  
  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Danger Theme</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Danger</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content>
        <ModalDialog.Header icon="trash" theme="danger">
          <ModalDialog.Title>Delete Item</ModalDialog.Title>
          <ModalDialog.Description>Danger theme for destructive actions</ModalDialog.Description>
        </ModalDialog.Header>
        <ModalDialog.Footer>
          <ModalDialog.Close>Cancel</ModalDialog.Close>
          <Button variant="primary" theme="danger">Delete</Button>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>
</div>`}
/>

### With Logo/Icon

Headers can display a logo or an icon:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-4">
    <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>With Logo</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header logo="jira">
        <ModalDialog.Title>Connect to Jira</ModalDialog.Title>
        <ModalDialog.Description>Set up your Jira integration</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Connect</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>

  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>With Icon</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header icon="branch-2">
        <ModalDialog.Title>Create Branch</ModalDialog.Title>
        <ModalDialog.Description>Create a new Git branch from main</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Create Branch</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>`}
/>

### Hidden Close Button

You can hide the close button in the top-right corner using the `hideClose` prop:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-4">
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Open Dialog</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content hideClose>
      <ModalDialog.Header>
        <ModalDialog.Title>Hidden Close Button</ModalDialog.Title>
        <ModalDialog.Description>This dialog has no close button in the top-right corner</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Footer>
        <ModalDialog.Close>Close</ModalDialog.Close>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>`}
/>

### Scrollable Content

The Body component automatically handles scrolling for long content:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col space-y-4">
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Default</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header icon="circle-plus">
        <ModalDialog.Title>Long Content</ModalDialog.Title>
        <ModalDialog.Description>Dialog with scrollable content</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>
        <div className="space-y-4">
          {Array.from({ length: 20 }).map((_, i) => (
            <div key={i}>
              <h3 className="font-body-strong">Section {i + 1}</h3>
              <p className="font-body-normal">Lorem ipsum dolor sit amet, consectetur adipiscing elit</p>
            </div>
          ))}
        </div>
      </ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Close</ModalDialog.Close>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>

  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Fixed Height</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content className="h-1/2">
      <ModalDialog.Header icon="circle-plus">
        <ModalDialog.Title>Long Content</ModalDialog.Title>
        <ModalDialog.Description>Dialog with scrollable content</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>
        <div className="space-y-4">
          {Array.from({ length: 20 }).map((_, i) => (
            <div key={i}>
              <h3 className="font-body-strong">Section {i + 1}</h3>
              <p className="font-body-normal">Lorem ipsum dolor sit amet, consectetur adipiscing elit</p>
            </div>
          ))}
        </div>
      </ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Close</ModalDialog.Close>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>`}
/>

## `Anatomy`

All parts of the `ModalDialog` component can be imported and composed as required.

```tsx
<ModalDialog.Root>
  <ModalDialog.Trigger />
  <ModalDialog.Content>
    <ModalDialog.Header>
      <ModalDialog.Title />
      <ModalDialog.Description />
    </ModalDialog.Header>
    <ModalDialog.Body />
    <ModalDialog.Footer>
      <ModalDialog.Close />
    </ModalDialog.Footer>
  </ModalDialog.Content>
</ModalDialog.Root>
```

## API Reference

### `Root`

Controls the open/close behavior of the modal.

<DocsPage.PropsTable
  props={[
    {
      name: "open",
      description: "Controls the open state of the dialog",
      required: false,
      value: "boolean",
    },
    {
      name: "onOpenChange",
      description: "Callback when open state changes",
      required: false,
      value: "(open: boolean) => void",
    },
    {
      name: "children",
      description: "Modal content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Content`

The main content container of the dialog.

<DocsPage.PropsTable
  props={[
    {
      name: "size",
      description: "Controls the width of the dialog",
      required: false,
      value: "'sm' | 'md' | 'lg'",
      defaultValue: "'sm'",
    },
    {
      name: "hideClose",
      description: "Hides the close button in the top-right corner",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Dialog sections",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Header`

The header section of the dialog, containing the title and optional description.

<DocsPage.PropsTable
  props={[
    {
      name: "icon",
      description: "Icon to display in the header (cannot be used with logo)",
      required: false,
      value: "string",
    },
    {
      name: "logo",
      description: "Logo to display in the header (cannot be used with icon)",
      required: false,
      value: "string",
    },
    {
      name: "theme",
      description: "Theme for the header section",
      required: false,
      value: "'default' | 'warning' | 'danger'",
      defaultValue: "'default'",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Header sections",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Title`

The title text component for the dialog header.

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Title content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Description`

Optional description text below the title.

<Aside>
  The `Description` component serves two purposes: it provides visible
  descriptive text and is also used for accessibility, describing the modal to
  screen readers. If you omit it, you should use the `aria-describedby`
  attribute instead, pointing to the ID of element(s) that describe the dialog's
  purpose.
</Aside>

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Description content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Description content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Body`

Main scrollable content area of the dialog.

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Body content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Body content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Footer`

Footer section for actions (e.g., buttons).

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Footer content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Footer content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Close`

Can be used inside `Footer` or as a standalone button to close the modal.

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Button label",
      required: true,
      value: "ReactNode",
    },
  ]}
/>
