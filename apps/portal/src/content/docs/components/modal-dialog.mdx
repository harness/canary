---
title: ModalDialog
description: A dialog overlay that appears above the page content
beta: true
---

The `ModalDialog` component provides an accessible and customizable dialog overlay for displaying important content or requesting user input. It supports different sizes, themes, and can include icons or logos in the header.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-4">
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Basic Modal</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header logo="gitlab">
        <ModalDialog.Title>Basic Dialog</ModalDialog.Title>
        <ModalDialog.Description>A simple dialog with logo</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>
        Content goes here
      </ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Close</ModalDialog.Close>
        <Button variant="primary">Confirm</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>

  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Danger Modal</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header icon="incidents" theme="danger">
        <ModalDialog.Title>Danger</ModalDialog.Title>
        <ModalDialog.Description>A danger dialog with icon</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>
        Danger content here
      </ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary" theme="danger">Confirm</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>`}
/>

## Usage

```typescript jsx
import { ModalDialog } from "@harnessio/ui/components";

// ...

return (
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Open Dialog</Button>
    </ModalDialog.Trigger>

    <ModalDialog.Content size="md">
      <ModalDialog.Header icon="circle-plus">
        <ModalDialog.Title>Dialog Title</ModalDialog.Title>
        <ModalDialog.Description>Optional description text</ModalDialog.Description>
      </ModalDialog.Header>

      <ModalDialog.Body>
        Dialog content goes here
      </ModalDialog.Body>

      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Confirm</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
)
```

### Sizes

The dialog supports three sizes: small, medium, and large:

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-4">
  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Small (Default)</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Small Dialog</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content size="sm">
        <ModalDialog.Header>
          <ModalDialog.Title>Small Dialog</ModalDialog.Title>
        </ModalDialog.Header>
        <ModalDialog.Body>Compact size for simple dialogs</ModalDialog.Body>
        <ModalDialog.Footer>
          <ModalDialog.Close>Close</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>

<div className="space-y-2">
  <h3 className="text-lg font-semibold">Medium</h3>
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Medium Dialog</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content size="md">
      <ModalDialog.Header>
        <ModalDialog.Title>Medium Dialog</ModalDialog.Title>
      </ModalDialog.Header>
      <ModalDialog.Body>Standard size for most use cases</ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Close</ModalDialog.Close>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>

  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Large</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Large Dialog</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content size="lg">
        <ModalDialog.Header>
          <ModalDialog.Title>Large Dialog</ModalDialog.Title>
        </ModalDialog.Header>
        <ModalDialog.Body>Maximum width for complex content</ModalDialog.Body>
        <ModalDialog.Footer>
          <ModalDialog.Close>Close</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>
</div>`}
/>

### Themes

Headers can have different themes with matching icons:

<DocsPage.ComponentExample
  client:only
  code={`<div className="space-y-4">
  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Default Theme</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Default</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content>
        <ModalDialog.Header icon="circle-plus">
          <ModalDialog.Title>Information</ModalDialog.Title>
          <ModalDialog.Description>Default theme with info icon</ModalDialog.Description>
        </ModalDialog.Header>
        <ModalDialog.Body>Content here</ModalDialog.Body>
        <ModalDialog.Footer>
          <ModalDialog.Close>Close</ModalDialog.Close>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>

<div className="space-y-2">
  <h3 className="text-lg font-semibold">Warning Theme</h3>
  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>Warning</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header icon="circle-plus" theme="warning">
        <ModalDialog.Title>Warning</ModalDialog.Title>
        <ModalDialog.Description>
          Warning theme with caution icon
        </ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>Content here</ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Continue</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>

  <div className="space-y-2">
    <h3 className="text-lg font-semibold">Danger Theme</h3>
    <ModalDialog.Root>
      <ModalDialog.Trigger asChild>
        <Button>Danger</Button>
      </ModalDialog.Trigger>
      <ModalDialog.Content>
        <ModalDialog.Header icon="circle-plus" theme="danger">
          <ModalDialog.Title>Delete Item</ModalDialog.Title>
          <ModalDialog.Description>Danger theme for destructive actions</ModalDialog.Description>
        </ModalDialog.Header>
        <ModalDialog.Body>This action cannot be undone</ModalDialog.Body>
        <ModalDialog.Footer>
          <ModalDialog.Close>Cancel</ModalDialog.Close>
          <Button variant="danger">Delete</Button>
        </ModalDialog.Footer>
      </ModalDialog.Content>
    </ModalDialog.Root>
  </div>
</div>`}
/>

### With Logo/Icon

Headers can display a logo or an icon:

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-4">
    <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>With Logo</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header logo="jira">
        <ModalDialog.Title>Connect to Jira</ModalDialog.Title>
        <ModalDialog.Description>Set up your Jira integration</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>
        Integration setup form would go here
      </ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Connect</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>

  <ModalDialog.Root>
    <ModalDialog.Trigger asChild>
      <Button>With Icon</Button>
    </ModalDialog.Trigger>
    <ModalDialog.Content>
      <ModalDialog.Header icon="branch-2">
        <ModalDialog.Title>Create Branch</ModalDialog.Title>
        <ModalDialog.Description>Create a new Git branch from main</ModalDialog.Description>
      </ModalDialog.Header>
      <ModalDialog.Body>
        Branch creation form would go here
      </ModalDialog.Body>
      <ModalDialog.Footer>
        <ModalDialog.Close>Cancel</ModalDialog.Close>
        <Button variant="primary">Create Branch</Button>
      </ModalDialog.Footer>
    </ModalDialog.Content>
  </ModalDialog.Root>
</div>`}
/>

### Scrollable Content

The Body component automatically handles scrolling for long content:

<DocsPage.ComponentExample
  client:only
  code={`<ModalDialog.Root>
  <ModalDialog.Trigger asChild>
    <Button>Scrollable</Button>
  </ModalDialog.Trigger>
  <ModalDialog.Content>
    <ModalDialog.Header icon="circle-plus">
      <ModalDialog.Title>Long Content</ModalDialog.Title>
      <ModalDialog.Description>Dialog with scrollable content</ModalDialog.Description>
    </ModalDialog.Header>
    <ModalDialog.Body>
      <div className="space-y-4">
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i}>
            <h3 className="font-semibold">Section {i + 1}</h3>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit...</p>
          </div>
        ))}
      </div>
    </ModalDialog.Body>
    <ModalDialog.Footer>
      <ModalDialog.Close>Close</ModalDialog.Close>
    </ModalDialog.Footer>
  </ModalDialog.Content>
</ModalDialog.Root>`}
/>

<DocsPage.PropsTable
  props={[
    {
      name: "size",
      type: "'sm' | 'md' | 'lg'",
      default: "'sm'",
      description: "Controls the width of the dialog",
    },
    {
      name: "hideClose",
      type: "boolean",
      default: "false",
      description: "Hides the close button in the top-right corner",
    },
    {
      name: "theme",
      type: "'default' | 'warning' | 'danger'",
      default: "'default'",
      description: "Theme for the header section",
    },
    {
      name: "icon",
      type: "string",
      description: "Icon to display in the header (cannot be used with logo)",
    },
    {
      name: "logo",
      type: "string",
      description: "Logo to display in the header (cannot be used with icon)",
    },
    {
      name: "open",
      type: "boolean",
      description: "Controls the open state in controlled mode",
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      description: "Callback when open state changes",
    },
  ]}
/>

## API Reference

### Root

Controls the open/close behavior of the modal.

```tsx
<ModalDialog.Root open={isOpen} onOpenChange={setIsOpen}>
  {/* Modal content */}
</ModalDialog.Root>
```

<DocsPage.PropsTable
  props={[
    {
      name: "open",
      description: "Controls the open state of the dialog",
      required: false,
      value: "boolean",
    },
    {
      name: "defaultOpen",
      description: "Initial open state for uncontrolled mode",
      required: false,
      value: "boolean",
    },
    {
      name: "onOpenChange",
      name: "onOpenChange",
      description: "Callback when open state changes",
      required: false,
      value: "(open: boolean) => void",
    },
    {
      name: "children",
      description: "Modal content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Content

The main content container of the dialog.

```tsx
<ModalDialog.Content size="md" hideClose={false}>
  {/* Dialog sections */}
</ModalDialog.Content>
```

<DocsPage.PropsTable
  props={[
    {
      name: "size",
      description: "Controls the width of the dialog",
      required: false,
      value: "'sm' | 'md' | 'lg'",
      default: "'sm'",
    },
    {
      name: "hideClose",
      description: "Hides the close button in the top-right corner",
      required: false,
      value: "boolean",
      default: "false",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Dialog sections",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Header

The header section of the dialog, containing the title and optional description.

```tsx
<ModalDialog.Header icon="circle-plus" theme="warning">
  <ModalDialog.Title>Dialog Title</ModalDialog.Title>
  <ModalDialog.Description>Optional description</ModalDialog.Description>
</ModalDialog.Header>
```

<DocsPage.PropsTable
  props={[
    {
      name: "icon",
      description: "Icon to display in the header (cannot be used with logo)",
      required: false,
      value: "string",
    },
    {
      name: "logo",
      description: "Logo to display in the header (cannot be used with icon)",
      required: false,
      value: "string",
    },
    {
      name: "theme",
      description: "Theme for the header section",
      required: false,
      value: "'default' | 'warning' | 'danger'",
      default: "'default'",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Header sections",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Title

The title text component for the dialog header.

```tsx
<ModalDialog.Title className="custom-class">Dialog Title</ModalDialog.Title>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Title content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Description

Optional description text below the title.

```tsx
<ModalDialog.Description className="custom-class">
  Additional context or instructions
</ModalDialog.Description>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Description content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Body

Main scrollable content area of the dialog.

```tsx
<ModalDialog.Body className="custom-class">
  {/* Body content */}
</ModalDialog.Body>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Body content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Footer

Footer section for actions (e.g., buttons).

```tsx
<ModalDialog.Footer className="custom-class">
  <ModalDialog.Close>Cancel</ModalDialog.Close>
  <Button variant="primary">Confirm</Button>
</ModalDialog.Footer>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Footer content",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### Close

Can be used inside `Footer` or as a standalone button to close the modal.

```tsx
<ModalDialog.Close>Cancel</ModalDialog.Close>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Button label",
      required: true,
      value: "ReactNode",
    },
  ]}
/>
