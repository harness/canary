---
title: MultiSelect V2
description: MultiSelect component
beta: true
---

The `MultiSelect` component allows users to select multiple options from a dropdown list. It is useful for scenarios where multiple selections are needed.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex w-full flex-col flex-wrap gap-4 items-baseline">
    <div>MultipleSelector with key-value support</div>
    <div className="relative w-full">
      <MultiSelectV2.MultiSelect
        label="Frameworks"
        defaultValue={[{ key: 'React' }, { key: 'category', value: 'frontend' }]}
        options={[
          { key: 'nextjs' },
          { key: 'Vite' },
          { key: 'Nuxt' },
          { key: 'Vue' },
        ]}
        onChange={(selected) => console.log(selected)}
        placeholder="Select frameworks you like..."
        caption="Separate tags with commas or press Enter. Use the format key:value for object entries."
        disallowCreation
      />
    </div>
</div>`}
/>

## Usage

```tsx
import { MultipleSelector, MultiSelectOption } from "@harnessio/ui/components";
import { useState } from "react";

// Basic usage with controlled component
const MyComponent = () => {
  const [selected, setSelected] = useState<MultiSelectOption[]>([]);

  return (
    <MultipleSelector
      label="Select frameworks"
      placeholder="Select options..."
      options={[{ key: "react" }, { key: "vue" }, { key: "angular" }]}
      value={selected}
      onChange={setSelected}
    />
  );
};
```

## MultiSelectOption Type

```typescript
interface MultiSelectOption {
  key: string; // Required - the main identifier for the option
  value?: string; // Optional - used for key-value pairs
  disable?: boolean; // Optional - disables the option in the dropdown
  onReset?: () => void; // Optional - custom reset handler for the option
}
```

## API Reference

<DocsPage.PropsTable
  props={[
    {
      name: "label",
      description: "Label displayed above the component",
      type: "string",
      required: false,
    },
    {
      name: "caption",
      description: "Helper text displayed below the component",
      type: "string",
      required: false,
    },
    {
      name: "value",
      description: "Selected options (controlled mode)",
      type: "MultiSelectOption[]",
      required: false,
    },
    {
      name: "defaultValue",
      description: "Initial selected options (uncontrolled mode)",
      type: "MultiSelectOption[]",
      required: false,
      default: "[]",
    },
    {
      name: "options",
      description: "Available options to select from",
      type: "MultiSelectOption[]",
      required: false,
    },
    {
      name: "placeholder",
      description: "Placeholder text when no options are selected",
      type: "string",
      required: false,
    },
    {
      name: "searchQuery",
      description: "Current search query string (controlled search)",
      type: "string | null",
      required: false,
    },
    {
      name: "setSearchQuery",
      description: "Callback to update search query",
      type: "(query: string | null) => void",
      required: false,
    },
    {
      name: "onChange",
      description: "Callback when selected options change",
      type: "(options: MultiSelectOption[]) => void",
      required: false,
    },
    {
      name: "maxSelected",
      description: "Maximum number of items that can be selected",
      type: "number",
      required: false,
      default: "Number.MAX_SAFE_INTEGER",
    },
    {
      name: "onMaxSelected",
      description: "Callback when max selection limit is reached",
      type: "(maxLimit: number) => void",
      required: false,
    },
    {
      name: "disabled",
      description: "Disables the component",
      type: "boolean",
      required: false,
      default: "false",
    },
    {
      name: "className",
      description: "Additional CSS class names",
      type: "string",
      required: false,
    },
    {
      name: "disallowCreation",
      description: "Prevents creating new options",
      type: "boolean",
      required: false,
      default: "false",
    },
    {
      name: "isLoading",
      description: "Shows loading state in the dropdown",
      type: "boolean",
      required: false,
      default: "false",
    },
  ]}
/>
