---
title: Stepper
description: A vertical stepper component for multi-step flows with nested steps, state indicators, and navigation guards.
beta: true
---

import { DocsPage } from "@/components/docs-page";
import { Aside } from "@astrojs/starlight/components";

The `Stepper` component provides a vertical progress indicator for multi-step workflows like wizards and onboarding flows. It supports dynamic nested steps, multiple visual states, transition animations, navigation guards, and keyboard navigation.

It is composed of `Stepper.Root`, `Stepper.StepGroup`, and `Stepper.Step` to offer a structured and customizable interface.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("connect");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="Pipeline Setup">
                <Stepper.Step value="connect" title="Connect" description="Choose your provider" />
                <Stepper.Step value="configure" title="Configure" description="Set up your pipeline" />
                <Stepper.Step value="review" title="Review" description="Confirm and create" />
            </Stepper.Root>
        </div>
    );

}`}
/>

## Usage

```typescript jsx
import { Stepper } from "@harnessio/ui/components";

//...

const [currentStep, setCurrentStep] = useState("step-1");

return (
  <Stepper.Root value={currentStep} onValueChange={setCurrentStep} title="Setup">
    <Stepper.Step value="step-1" title="First Step" description="Do something" />
    <Stepper.Step value="step-2" title="Second Step" description="Do another thing" />
    <Stepper.Step value="step-3" title="Final Step" description="All done" />
  </Stepper.Root>
)
```

## Anatomy

All parts of the `Stepper` component can be imported and composed as required. A `Stepper.Step` rendered directly under `Stepper.Root` is a top-level step; a `Stepper.Step` rendered inside a `Stepper.StepGroup` is a nested step.

```typescript jsx
<Stepper.Root>
  <Stepper.StepGroup>
    <Stepper.Step />
    <Stepper.Step />
  </Stepper.StepGroup>
  <Stepper.Step />
</Stepper.Root>
```

## Step States

Steps display different visual states based on their position relative to the active step and explicit state overrides.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("active");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="Step States">
                <Stepper.Step value="completed" title="Completed" description="This step is done" />
                <Stepper.Step value="active" title="Active" description="Currently on this step" />
                <Stepper.Step value="upcoming" title="Upcoming" description="Not yet reached" />
                <Stepper.Step value="error-step" title="Error" description="Something went wrong" state="error" />
            </Stepper.Root>
        </div>
    );

}`}
/>

<Aside type="tip">
  Step states are derived automatically based on position (completed for steps
  before active, upcoming for steps after), but can be explicitly overridden
  with the `state` prop to show `error` or `skipped`.
</Aside>

## Loading and Blocking

Steps can show a loading spinner and optionally block forward navigation while an async operation is in progress.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("checking");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="Connectivity Check">
                <Stepper.Step value="provider" title="Select Provider" description="Choose VCS" />
                <Stepper.Step
                    value="checking"
                    title="Checking Connection"
                    description="Verifying access..."
                    loading
                    blocking
                />
                <Stepper.Step value="repo" title="Select Repository" description="Pick your repo" />
            </Stepper.Root>
        </div>
    );

}`}
/>

<Aside type="note">
  When `blocking` is set on the active step, navigation to subsequent steps is
  prevented. The user can still navigate backwards. Combine with `loading` to
  show a spinner on the indicator.
</Aside>

## Nested Steps

A `Stepper.StepGroup` can contain dynamic nested steps that appear when the group is active. Nested steps support conditional rendering based on application state.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("auth");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="GitHub Integration">
                <Stepper.StepGroup value="connect" title="Connect" description="Choose provider">
                    <Stepper.Step value="auth" title="Authenticate" description="OAuth with GitHub" />
                    <Stepper.Step value="test" title="Test Connection" description="Verify access" />
                </Stepper.StepGroup>
                <Stepper.Step value="configure" title="Configure" description="Set up pipeline" />
                <Stepper.Step value="done" title="Done" description="All set" />
            </Stepper.Root>
        </div>
    );

}`}
/>

<Aside type="tip">
  Nested steps are conditionally rendered — they only appear when their parent
  step group is active. The `value` passed to `Stepper.Root` can be either a
  step-group value or a nested-step value.
</Aside>

## Indeterminate Nested Steps

When a step group has `hasNestedSteps` but no children are provided, a placeholder indicator with "..." is shown, signaling that nested steps will be determined dynamically (e.g., after an API call resolves).

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("connectivity");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="Build pipeline setup">
                <Stepper.Step value="vcs" title="Select Version Control" description="Description" state="completed" />
                <Stepper.StepGroup value="connectivity" title="Check Connectivity" description="Description" />
                <Stepper.Step value="repo" title="Select a repository" description="Description" />
                <Stepper.StepGroup value="analysis" title="Code Analysis" description="Description" hasNestedSteps />
                <Stepper.Step value="generate" title="Generate Pipeline" description="Description" />
            </Stepper.Root>
        </div>
    );

}`}
/>

Here's an example combining real nested steps (`.1`, `.2`, `.3`) on the active step group with an indeterminate placeholder on a following step group:

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("scan-deps");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="Build pipeline setup">
                <Stepper.Step value="vcs" title="Select Version Control" description="Description" state="completed" />
                <Stepper.Step value="connectivity" title="Check Connectivity" description="Description" state="completed" />
                <Stepper.StepGroup value="analysis" title="Code Analysis" description="Description" hasNestedSteps>
                    <Stepper.Step value="scan-deps" title="Scan Dependencies" description="Check for vulnerabilities" />
                    <Stepper.Step value="lint" title="Run Linter" description="Static analysis" />
                    <Stepper.Step value="coverage" title="Coverage Report" description="Verify thresholds" />
                </Stepper.StepGroup>
                <Stepper.StepGroup value="generate" title="Generate Pipeline" description="Description" hasNestedSteps />
            </Stepper.Root>
        </div>
    );

}`}
/>

<Aside type="note">
  The `hasNestedSteps` prop reserves space for nested steps even when none are
  rendered yet. This is useful when nested steps are loaded asynchronously or
  depend on a previous step's outcome.
</Aside>

## Navigation Guard

Use the `onBeforeChange` prop to confirm navigation (e.g., warn users about losing progress when going back).

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("configure");
    const steps = ["connect", "configure", "review"];

    return (
        <div className="w-[300px]">
            <Stepper.Root
                value={step}
                onValueChange={setStep}
                title="With Guard"
                onBeforeChange={(from, to) => {
                    const fromIdx = steps.indexOf(from);
                    const toIdx = steps.indexOf(to);
                    if (toIdx < fromIdx) {
                        return "Going back will reset your progress. Are you sure?";
                    }
                    return true;
                }}
            >
                <Stepper.Step value="connect" title="Connect" description="Choose provider" />
                <Stepper.Step value="configure" title="Configure" description="Set up pipeline" />
                <Stepper.Step value="review" title="Review" description="Confirm settings" />
            </Stepper.Root>
        </div>
    );

}`}
/>

<Aside type="note">
  `onBeforeChange` receives `(fromValue, toValue)` and should return: - `true`
  to allow navigation - `false` to silently block navigation - A `string` to
  show a confirmation dialog with that message
</Aside>

## Completed State

When all steps are done, pass `completed` to show a "Complete" badge instead of the step counter.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("review");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="All Done" completed>
                <Stepper.Step value="connect" title="Connect" description="Provider selected" />
                <Stepper.Step value="configure" title="Configure" description="Pipeline configured" />
                <Stepper.Step value="review" title="Review" description="Confirmed" />
            </Stepper.Root>
        </div>
    );

}`}
/>

## Without Connectors

Set `showConnectors={false}` to hide the vertical connector lines between steps.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [step, setStep] = React.useState("first");

    return (
        <div className="w-[300px]">
            <Stepper.Root value={step} onValueChange={setStep} title="No Connectors" showConnectors={false}>
                <Stepper.Step value="first" title="First" description="Step one" />
                <Stepper.Step value="second" title="Second" description="Step two" />
                <Stepper.Step value="third" title="Third" description="Step three" />
            </Stepper.Root>
        </div>
    );

}`}
/>

## Skeleton Loading

When no children are passed, the stepper renders a skeleton placeholder. Control the number of skeleton items with `skeletonCount`.

<DocsPage.ComponentExample
  client:only
  code={`<div className="w-[300px]">
    <Stepper.Root value="" onValueChange={() => {}} title="Loading..." skeletonCount={4} />
</div>`}
/>

## API Reference

### `Root`

The root container that manages stepper state, provides context to children, and renders the header with title and progress counter.

```typescript jsx
<Stepper.Root
  value={currentStep}            // active step or nested-step value
  onValueChange={setStep}        // callback when step changes
  title="Setup"                  // [OPTIONAL] displayed in the header
  completed={false}              // [OPTIONAL] shows "Complete" instead of counter
  showConnectors={true}          // [OPTIONAL] show connector lines between steps
  onBeforeChange={guardFn}       // [OPTIONAL] navigation guard
  skeletonCount={3}              // [OPTIONAL] skeleton items when no children
  className="my-class"           // [OPTIONAL] custom class on the nav element
>
  {/* Step / StepGroup components */}
</Stepper.Root>
```

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description: "The active step or nested-step value (controlled)",
      required: true,
      value: "string",
    },
    {
      name: "onValueChange",
      description: "Callback fired when the user navigates to a different step",
      required: true,
      value: "(value: string) => void",
    },
    {
      name: "title",
      description: "Title displayed in the stepper header",
      required: false,
      value: "string",
    },
    {
      name: "completed",
      description: "When true, shows 'Complete' instead of step counter",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "showConnectors",
      description: "Show vertical connector lines between steps",
      required: false,
      value: "boolean",
      defaultValue: "true",
    },
    {
      name: "onBeforeChange",
      description:
        "Navigation guard. Return true to allow, false to block, or a string to show a confirmation dialog.",
      required: false,
      value: "(from: string, to: string) => boolean | string",
    },
    {
      name: "skeletonCount",
      description:
        "Number of skeleton items to show when no children are provided",
      required: false,
      value: "number",
      defaultValue: "3",
    },
    {
      name: "className",
      description: "Custom class applied to the root nav element",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Stepper.Step / Stepper.StepGroup components",
      required: false,
      value: "ReactNode",
    },
  ]}
/>

### `StepGroup`

A step that groups nested steps. Renders the indicator, title, description, connector, and optional nested `Stepper.Step` children.

```typescript jsx
<Stepper.StepGroup
  value="connect"                // unique identifier for this step group
  title="Connect"                // step title (string or ReactNode)
  description="Choose provider"  // [OPTIONAL] description text
  state="error"                  // [OPTIONAL] explicit state override
  loading={false}                // [OPTIONAL] show spinner on indicator
  blocking={false}               // [OPTIONAL] block forward navigation
  disabled={false}               // [OPTIONAL] prevent interaction
  hasNestedSteps={false}         // [OPTIONAL] reserve nested-step space
  className="my-class"           // [OPTIONAL] custom class
>
  {/* Optional Stepper.Step children (nested steps) */}
</Stepper.StepGroup>
```

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description: "Unique identifier for this step group",
      required: true,
      value: "string",
    },
    {
      name: "title",
      description: "Step title displayed next to the indicator",
      required: true,
      value: "string | ReactNode",
    },
    {
      name: "description",
      description: "Secondary text below the title",
      required: false,
      value: "string",
    },
    {
      name: "state",
      description: "Explicit state override (error or skipped)",
      required: false,
      value: "'error' | 'skipped'",
    },
    {
      name: "loading",
      description: "Show a loading spinner on the indicator when active",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "blocking",
      description: "When active, prevents navigation to subsequent steps",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "disabled",
      description: "Prevents interaction with this step group",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "hasNestedSteps",
      description:
        "Reserve vertical space for nested steps (useful when nested steps load asynchronously)",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "className",
      description: "Custom class applied to the step button",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description:
        "Stepper.Step components (rendered when this step group is active)",
      required: false,
      value: "ReactNode",
    },
  ]}
/>

### `Step`

An individual step. Rendered directly under `Stepper.Root`, it behaves as a top-level step; rendered inside a `Stepper.StepGroup`, it behaves as a nested step and is only rendered when the parent step group is active.

```typescript jsx
<Stepper.Step
  value="auth"                    // unique identifier for this step
  title="Authenticate"           // step title
  description="OAuth with GitHub" // [OPTIONAL] description text
  state="error"                  // [OPTIONAL] explicit state override
  disabled={false}               // [OPTIONAL] prevent interaction
  className="my-class"           // [OPTIONAL] custom class
/>
```

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description: "Unique identifier for this step",
      required: true,
      value: "string",
    },
    {
      name: "title",
      description: "Step title",
      required: true,
      value: "string | ReactNode",
    },
    {
      name: "description",
      description: "Secondary text below the title",
      required: false,
      value: "string",
    },
    {
      name: "state",
      description: "Explicit state override (error or skipped)",
      required: false,
      value: "'error' | 'skipped'",
    },
    {
      name: "disabled",
      description: "Prevents interaction with this step",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "visualCompleted",
      description:
        "Presentation-only: renders this step's icon/color as completed regardless of the real state. Does not affect accordion-open behavior.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "showStepBadge",
      description:
        "Renders a 'Step {n}/{total}' pill badge next to the step title. Only meaningful when this Step has no parent StepGroup (top-level registration).",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "totalStepsOverride",
      description:
        "Overrides the announced/displayed step total. Useful when the currently-registered step count (progressive disclosure) undercounts the flow's real total.",
      required: false,
      value: "number",
    },
    {
      name: "className",
      description: "Custom class applied to the step button",
      required: false,
      value: "string",
    },
  ]}
/>

## Accessibility

The stepper implements the following accessibility features:

- **ARIA**: Uses `aria-current="step"` on the active step, `aria-label` on each step button with position context, and `aria-disabled` for disabled steps
- **Live Region**: Announces step changes via `aria-live="polite"` for screen readers
- **Keyboard Navigation**: Supports ArrowDown, ArrowUp, Home, and End keys with roving tabindex
- **Reduced Motion**: Respects `prefers-reduced-motion` by disabling transition animations

## Best Practices

1. **Unique Values**: Every step and nested step must have a unique `value` across the entire stepper
2. **Controlled State**: The component is always controlled — provide both `value` and `onValueChange`
3. **Blocking Steps**: Use `blocking` for async operations that must complete before proceeding (e.g., connectivity checks)
4. **Navigation Guards**: Use `onBeforeChange` to warn about data loss, not to enforce linear progression
5. **Nested Step Cleanup**: When nested steps unmount (e.g., provider changes), the stepper automatically falls back to the parent step-group value
6. **Skeleton State**: Pass no children to show a skeleton — useful while step configuration loads asynchronously
