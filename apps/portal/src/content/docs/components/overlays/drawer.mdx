---
title: Drawer
description: Drawer component with stacking
beta: true
---

The `Drawer` component provides a way to display content in a panel that slides in from the edge of the screen. Supports multiple configuration options, including direction (left, right, top, bottom), size constraints, and nested drawers for building complex, multi-level interfaces.

It is composed of several subcomponents such as `Drawer.Root`, `Drawer.Trigger`, `Drawer.Content`, `Drawer.Header`, `Drawer.Tagline`, `Drawer.Title`, `Drawer.Description`, `Drawer.Body`, `Drawer.Footer`, `Drawer.DualPane`, `Drawer.Rail`, `Drawer.Steps`, `Drawer.Step`, `Drawer.SubStep`, `Drawer.DualPaneMain` and `Drawer.Close` to offer a structured and customizable interface.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`
    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer</Button>
      </Drawer.Trigger>

      <Drawer.Content>
        <Drawer.Header icon="repository">
          <Drawer.Tagline>tagline</Drawer.Tagline>
          <Drawer.Title>Title</Drawer.Title>
          <Drawer.Description>
            Description
          </Drawer.Description>
        </Drawer.Header>

        <Drawer.Body>
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur at sagittis justo.
            Integer vehicula turpis at justo gravida, at convallis lorem tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur at sagittis justo.
            Integer vehicula turpis at justo gravida, at convallis lorem tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur at sagittis justo.
            Integer vehicula turpis at justo gravida, at convallis lorem tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
        </Drawer.Body>

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Primary>
              <Button>Action</Button>
            </ButtonLayout.Primary>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

`}
/>

## Usage

```typescript jsx
import { Drawer } from '@harnessio/ui/components'

//...

return (
  <Drawer.Root
    direction="left"
    open={isDrawerOpen}
    onOpenChange={setIsDrawerOpen}
  >
    <Drawer.Trigger>
      <Button>Open drawer</Button>
    </Drawer.Trigger>

    <Drawer.Content size="md">
      <Drawer.Header icon="info-circle-solid" hideClose>
        <Drawer.Tagline>Section</Drawer.Tagline>
        <Drawer.Title>Title of the drawer</Drawer.Title>
        <Drawer.Description>Description of the drawer</Drawer.Description>
      </Drawer.Header>

      <Drawer.Body className="">
        Content of the drawer goes here

        <Drawer.Root>
          <Drawer.Trigger>
            <Button>Open Nested Drawer</Button>
          </Drawer.Trigger>

          <Drawer.Content>
            <Drawer.Header>
              <Drawer.Title>Title</Drawer.Title>
            </Drawer.Header>

            <Drawer.Body>
              Content
            </Drawer.Body>

            <Drawer.Footer>
              Footer
            </Drawer.Footer>
          </Drawer.Content>
        </Drawer.Root>
      </Drawer.Body>

      <Drawer.Footer>
        <ButtonLayout.Root>
          <ButtonLayout.Primary>
            <Button>Action</Button>
          </ButtonLayout.Primary>
          <ButtonLayout.Secondary>
            <Drawer.Close asChild>
              <Button variant="outline">Cancel</Button>
            </Drawer.Close>
          </ButtonLayout.Secondary>
        </ButtonLayout.Root>
      </Drawer.Footer>
    </Drawer.Content>
  </Drawer.Root>
)
```

## Anatomy

All parts of the `Drawer` component can be imported and composed as required.

```typescript jsx
<Drawer.Root>
  <Drawer.Trigger />
  <Drawer.Content>
    <Drawer.Header>
      <Drawer.Tagline />
      <Drawer.Title />
      <Drawer.Description />
    </Drawer.Header>
    <Drawer.Body />
    <Drawer.Footer>
      <Drawer.Close />
    </Drawer.Footer>
  </Drawer.Content>
</Drawer.Root>
```

## Nested drawers

Nested drawers can be implemented by placing a `Drawer.Root` inside the parent `Drawer.Content`.

When a wider drawer opens over a narrower one, the narrower drawer automatically expands to peek out from behind, preserving the visual stacking effect.

<DocsPage.ComponentExample
  client:only
  code={`
    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer (sm)</Button>
      </Drawer.Trigger>

      <Drawer.Content size="sm">
        <Drawer.Header>
          <Drawer.Title>Small Drawer</Drawer.Title>
          <Drawer.Description>
            This drawer will expand when a wider nested drawer opens
          </Drawer.Description>
        </Drawer.Header>

        <Drawer.Body>
          <Drawer.Root>
            <Drawer.Trigger>
              <Button>Open Nested Drawer (lg)</Button>
            </Drawer.Trigger>

            <Drawer.Content size="lg">
              <Drawer.Header>
                <Drawer.Title>Large Nested Drawer (2)</Drawer.Title>
              </Drawer.Header>

              <Drawer.Body>
                <Drawer.Root>
                  <Drawer.Trigger>
                    <Button>Open Nested Drawer (md)</Button>
                  </Drawer.Trigger>

                  <Drawer.Content size="md">
                    <Drawer.Header>
                      <Drawer.Title>Medium Nested Drawer (3)</Drawer.Title>
                    </Drawer.Header>

                    <Drawer.Body>
                      <Drawer.Root>
                        <Drawer.Trigger>
                          <Button>Open Nested Drawer (lg)</Button>
                        </Drawer.Trigger>

                        <Drawer.Content size="lg">
                          <Drawer.Header>
                            <Drawer.Title>Large Nested Drawer (4)</Drawer.Title>
                          </Drawer.Header>

                          <Drawer.Body>
                            <Drawer.Root>
                              <Drawer.Trigger>
                                <Button>Open Nested Drawer (md)</Button>
                              </Drawer.Trigger>

                              <Drawer.Content size="md">
                                <Drawer.Header>
                                  <Drawer.Title>Medium Nested Drawer (5)</Drawer.Title>
                                </Drawer.Header>

                                <Drawer.Body />
                              </Drawer.Content>
                            </Drawer.Root>
                          </Drawer.Body>
                        </Drawer.Content>
                      </Drawer.Root>
                    </Drawer.Body>
                  </Drawer.Content>
                </Drawer.Root>
              </Drawer.Body>
            </Drawer.Content>
          </Drawer.Root>

          <p className="mt-cn-md">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur at sagittis justo.
            Integer vehicula turpis at justo gravida, at convallis lorem tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur at sagittis justo.
            Integer vehicula turpis at justo gravida, at convallis lorem tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur at sagittis justo.
            Integer vehicula turpis at justo gravida, at convallis lorem tincidunt.
          </p>
          <p>
            Suspendisse potenti. Praesent in vestibulum lacus. Cras feugiat eros at ex cursus,
            vel consequat enim mattis. Quisque nec magna ac leo tempor scelerisque.
          </p>
          <p>
            Nullam tristique, justo vitae laoreet pretium, nisl nunc facilisis nulla, non porta erat sapien in justo.
            Sed nec massa nec turpis interdum volutpat. Morbi sed justo ac leo sagittis tincidunt.
          </p>
        </Drawer.Body>

        <Drawer.Footer>
          <ButtonLayout>
            <Button>Action</Button>
          </ButtonLayout>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

`}
/>

## Max stack depth

The `Drawer.Root` component accepts a `maxStackDepth` prop (default `7`) that controls how many nested drawers visually stack. Drawers beyond this limit still open normally but cover the deepest visible drawer instead of adding more visual offset.

<DocsPage.ComponentExample
  client:only
  code={`
    <Drawer.Root maxStackDepth={3}>
      <Drawer.Trigger>
        <Button>Open Drawer 1 (sm)</Button>
      </Drawer.Trigger>

      <Drawer.Content size="sm">
        <Drawer.Header>
          <Drawer.Title>Drawer 1 (sm)</Drawer.Title>
          <Drawer.Description>maxStackDepth is set to 3 — only 3 drawers visually stack</Drawer.Description>
        </Drawer.Header>

        <Drawer.Body>
          <Drawer.Root>
            <Drawer.Trigger>
              <Button>Open Drawer 2 (lg)</Button>
            </Drawer.Trigger>

            <Drawer.Content size="lg">
              <Drawer.Header>
                <Drawer.Title>Drawer 2 (lg)</Drawer.Title>
              </Drawer.Header>

              <Drawer.Body>
                <Drawer.Root>
                  <Drawer.Trigger>
                    <Button>Open Drawer 3 (md)</Button>
                  </Drawer.Trigger>

                  <Drawer.Content size="md">
                    <Drawer.Header>
                      <Drawer.Title>Drawer 3 (md)</Drawer.Title>
                      <Drawer.Description>Visual stacking limit reached — next drawer will cover this one</Drawer.Description>
                    </Drawer.Header>

                    <Drawer.Body>
                      <Drawer.Root>
                        <Drawer.Trigger>
                          <Button>Open Drawer 4 (lg)</Button>
                        </Drawer.Trigger>

                        <Drawer.Content size="lg">
                          <Drawer.Header>
                            <Drawer.Title>Drawer 4 (lg)</Drawer.Title>
                          </Drawer.Header>

                          <Drawer.Body>
                            <Drawer.Root>
                              <Drawer.Trigger>
                                <Button>Open Drawer 5 (md)</Button>
                              </Drawer.Trigger>

                              <Drawer.Content size="md">
                                <Drawer.Header>
                                  <Drawer.Title>Drawer 5 (md)</Drawer.Title>
                                </Drawer.Header>

                                <Drawer.Body>
                                  <p>This is the deepest drawer in the stack.</p>
                                </Drawer.Body>
                              </Drawer.Content>
                            </Drawer.Root>
                          </Drawer.Body>
                        </Drawer.Content>
                      </Drawer.Root>
                    </Drawer.Body>
                  </Drawer.Content>
                </Drawer.Root>
              </Drawer.Body>
            </Drawer.Content>
          </Drawer.Root>

          <p className="mt-cn-md">
            Open all 5 drawers to see the visual stacking cap in action.
            Drawers 1–3 will visually stack, but drawers 4 and 5 will cover drawer 3 instead of pushing it further back.
          </p>
        </Drawer.Body>
      </Drawer.Content>
    </Drawer.Root>

`}
/>

## Drawer sizes

The `Drawer.Content` component accepts a `size` prop to control its width (or height, depending on `direction`). Supported values are: `2xs`, `xs`, `sm`, `md`, `lg`, `xl`, and `full`.

<DocsPage.ComponentExample
  client:only
  code={`
  <ButtonLayout orientation="vertical">
    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with '2xs' size</Button>
      </Drawer.Trigger>

      <Drawer.Content size="2xs">
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'xs' size</Button>
      </Drawer.Trigger>

      <Drawer.Content size="xs">
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'sm' size</Button>
      </Drawer.Trigger>

      <Drawer.Content>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'md' size</Button>
      </Drawer.Trigger>

      <Drawer.Content size="md">
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'lg' size</Button>
      </Drawer.Trigger>

      <Drawer.Content size="lg">
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'xl' size</Button>
      </Drawer.Trigger>

      <Drawer.Content size="xl">
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'full' size</Button>
      </Drawer.Trigger>

      <Drawer.Content size="full">
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

  </ButtonLayout>
`}
/>

## Drawer opening direction

The `Drawer.Root` component accepts a `direction` prop to control which side the drawer opens from. Supported values are: `right`, `left`, `top`, and `bottom`.

<DocsPage.ComponentExample
  client:only
  code={`
  <ButtonLayout orientation="vertical">
    <Drawer.Root>
      <Drawer.Trigger>
        <Button>Open Drawer with 'right' direction</Button>
      </Drawer.Trigger>

      <Drawer.Content>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root direction="left">
      <Drawer.Trigger>
        <Button>Open Drawer with 'left' direction</Button>
      </Drawer.Trigger>

      <Drawer.Content>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root direction="top">
      <Drawer.Trigger>
        <Button>Open Drawer with 'top' direction</Button>
      </Drawer.Trigger>

      <Drawer.Content>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

    <Drawer.Root direction="bottom">
      <Drawer.Trigger>
        <Button>Open Drawer with 'bottom' direction</Button>
      </Drawer.Trigger>

      <Drawer.Content>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
        </Drawer.Header>

        <Drawer.Body />

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.Content>
    </Drawer.Root>

  </ButtonLayout>
`}
/>

### Keep focus on the custom trigger element

If a drawer is opened without the `Trigger` component (e.g., a dropdown item), focus will not return to the trigger element when the drawer is closed. To address this use `useCustomDialogTrigger` hook to register your custom trigger element.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [open, setOpen] = React.useState(false);
    const { triggerRef, registerTrigger } = useCustomDialogTrigger();

    const handleOpenDialog = () => {
      registerTrigger();
      setOpen(true);
    };

return (

  <>
    <Layout.Horizontal>
      <DropdownMenu.Root>
        <DropdownMenu.Trigger aria-label="options menu" ref={triggerRef} asChild>
          <Button>Open Menu</Button>
        </DropdownMenu.Trigger>

        <DropdownMenu.Content>
            <DropdownMenu.Item title="Open dialog" onSelect={handleOpenDialog} />
        </DropdownMenu.Content>

      </DropdownMenu.Root>

      <Drawer.Trigger>
        <Button onClick={handleOpenDialog}>Open Drawer</Button>
      </Drawer.Trigger>
    </Layout.Horizontal>

    <Drawer.Root open={open} onOpenChange={setOpen}>
      <Drawer.Content>
        <Drawer.Header icon="plus-circle">
          <Drawer.Title>Content</Drawer.Title>
          <Drawer.Description>Drawer opened from the custom trigger</Drawer.Description>
        </Drawer.Header>
        <Drawer.Body>
          <p className="font-body-normal">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit
          </p>
        </Drawer.Body>
        <Drawer.Footer>
          <Drawer.Close>Close</Drawer.Close>
        </Drawer.Footer>
      </Drawer.Content>

    </Drawer.Root>

</>
)}`}
/>

## Dual pane drawer

The dual pane drawer renders a fixed left rail beside a scrollable main pane. Use it whenever content benefits from a side-by-side layout, for example:

- **Multi-step flows** — a numbered step list in the rail and the active step's form in the main pane.
- **Reference while you work** — a glossary, definition list, file tree, related records, or other supporting content in the rail that the user can scan while completing a form, editor, or other primary task in the main pane.
- **Browse and detail** — a list of items in the rail with the selected item's detail view in the main pane.

The rail and the main pane each have their own scroll area, so users keep context on one side while moving through the other.

For non-stepped content (reference, browse, anything where the rail isn't a wizard), use `Drawer.Rail`. It provides the rail's chrome — fixed width, surface, border, optional title header, and an independent scroll area — and accepts arbitrary children, so you can drop in a definition list, file tree, navigation menu, or any other content.

For multi-step flows, compose the rail with `Drawer.Steps` and `Drawer.Step` — a numbered list with active / completed / upcoming states. Each step renders an indicator circle (check mark for completed, number for active or upcoming) plus a title and optional description. Put the active step content in `Drawer.DualPaneMain` with a separate scrollable `Drawer.Body`. Control the active step with `Drawer.Steps`' `value` and `onValueChange`. Any step the user has previously visited stays clickable — including steps ahead of the current one if they've already been reached — so the user can jump back and forth freely. Only steps the user has never visited are read-only.

For sub-flows inside a step, nest `Drawer.SubStep` children inside a `Drawer.Step`. Substeps appear under their parent step when the parent is active, with simpler indicators (check mark for completed, dot for active, dash for upcoming) and the same navigation rules: any visited substep stays clickable. Setting `Drawer.Steps` `value` to a substep's value automatically marks the parent step as active.

Use `size="md"`, `size="lg"`, or `size="xl"` on `Drawer.Content` so the rail and the main pane both have room. The panes never stack — the right pane stays beside the rail and shrinks with the viewport down to the `xs` drawer width as a [minimum](#sizes); past that point the dual pane scrolls horizontally. This applies at every `size` (including the fixed sizes), because `Drawer.Content` is also capped at the viewport's width.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [open, setOpen] = React.useState(false);
    const [currentStep, setCurrentStep] = React.useState('configuration');

    const steps = [
      { value: 'details', title: 'Step Title', description: 'Description' },
      { value: 'configuration', title: 'Step Title', description: 'Description' },
      { value: 'permissions', title: 'Step Title', description: 'Description' },
      { value: 'review', title: 'Step Title', description: 'Description' },
    ];

    const currentIndex = steps.findIndex((step) => step.value === currentStep);
    const activeStep = steps[currentIndex] ?? steps[0];

    const goNext = () => {
      if (currentIndex < steps.length - 1) {
        setCurrentStep(steps[currentIndex + 1].value);
      }
    };

    const goBack = () => {
      if (currentIndex > 0) {
        setCurrentStep(steps[currentIndex - 1].value);
      }
    };

    return (
      <Drawer.Root open={open} onOpenChange={setOpen}>
        <Drawer.Trigger>
          <Button>Open dual pane drawer</Button>
        </Drawer.Trigger>

        <Drawer.Content size="lg">
          <Drawer.DualPane>
            <Drawer.Steps
              value={currentStep}
              onValueChange={setCurrentStep}
              title="Title"
            >
              {steps.map((step) => (
                <Drawer.Step
                  key={step.value}
                  value={step.value}
                  title={step.title}
                  description={step.description}
                />
              ))}
            </Drawer.Steps>

            <Drawer.DualPaneMain>
              <Drawer.Header>
                <Drawer.Tagline>{currentIndex + 1} out of {steps.length} steps</Drawer.Tagline>
                <Drawer.Title>{activeStep.title}</Drawer.Title>
                {activeStep.description ? (
                  <Drawer.Description>{activeStep.description}</Drawer.Description>
                ) : null}
              </Drawer.Header>

              <Drawer.Body>
                <div className="flex h-full items-center justify-center">
                  <p className="font-heading-section text-cn-3">Content</p>
                </div>
              </Drawer.Body>

              <Drawer.Footer>
                <ButtonLayout.Root>
                  <ButtonLayout.Primary>
                    {currentIndex < steps.length - 1 ? (
                      <Button onClick={goNext}>
                        Continue
                        <IconV2 name="arrow-right" />
                      </Button>
                    ) : (
                      <Button>Finish</Button>
                    )}
                  </ButtonLayout.Primary>
                  <ButtonLayout.Secondary>
                    {currentIndex > 0 ? (
                      <Button variant="outline" onClick={goBack}>
                        <IconV2 name="arrow-left" />
                        Back
                      </Button>
                    ) : (
                      <Drawer.Close asChild>
                        <Button variant="outline">Cancel</Button>
                      </Drawer.Close>
                    )}
                  </ButtonLayout.Secondary>
                </ButtonLayout.Root>
              </Drawer.Footer>
            </Drawer.DualPaneMain>
          </Drawer.DualPane>
        </Drawer.Content>
      </Drawer.Root>
    );

}`}
/>

```typescript jsx
<Drawer.Root open={open} onOpenChange={setOpen}>
  <Drawer.Trigger>
    <Button>Open dual pane drawer</Button>
  </Drawer.Trigger>

  <Drawer.Content size="lg">
    <Drawer.DualPane>
      <Drawer.Steps value={currentStep} onValueChange={setCurrentStep} title="Title">
        <Drawer.Step value="details" title="Step Title" description="Description" />
        <Drawer.Step value="configuration" title="Step Title" description="Description" />
        <Drawer.Step value="permissions" title="Step Title" description="Description" />
        <Drawer.Step value="review" title="Step Title" description="Description" />
      </Drawer.Steps>

      <Drawer.DualPaneMain>
        <Drawer.Header>
          <Drawer.Tagline>{currentIndex + 1} out of {steps.length} steps</Drawer.Tagline>
          <Drawer.Title>{activeStep.title}</Drawer.Title>
          <Drawer.Description>{activeStep.description}</Drawer.Description>
        </Drawer.Header>

        <Drawer.Body>
          <div className="flex h-full items-center justify-center">
            <p className="font-heading-section text-cn-3">Content</p>
          </div>
        </Drawer.Body>

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Primary>
              <Button onClick={goNext}>Continue</Button>
            </ButtonLayout.Primary>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.DualPaneMain>
    </Drawer.DualPane>
  </Drawer.Content>
</Drawer.Root>
```

### Substeps

Nest `Drawer.SubStep` children inside a `Drawer.Step` to give that step its own sub-flow. Substeps appear under their parent when the parent is the active step (driven by setting `Drawer.Steps`' `value` to the parent's value or to any of its substep values). They use simpler indicators — check mark for completed, filled dot for active, dash for upcoming — and follow the same navigation rules as top-level steps: any substep the user has previously visited stays clickable so they can move freely between them, while substeps that have never been reached are read-only.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [open, setOpen] = React.useState(false);
    const [currentStep, setCurrentStep] = React.useState('step2.2');

    return (
      <Drawer.Root open={open} onOpenChange={setOpen}>
        <Drawer.Trigger>
          <Button>Open dual pane drawer with substeps</Button>
        </Drawer.Trigger>

        <Drawer.Content size="md">
          <Drawer.DualPane>
            <Drawer.Steps
              value={currentStep}
              onValueChange={setCurrentStep}
              title="Title"
              aria-label="Workspace creation steps"
            >
              <Drawer.Step value="step1" title="Step 1" description="Description" />
              <Drawer.Step value="step2" title="Step 2" description="Description">
                <Drawer.SubStep value="step2.1" title="Step 2.1" />
                <Drawer.SubStep value="step2.2" title="Step 2.2" />
                <Drawer.SubStep value="step2.3" title="Step 2.3" />
              </Drawer.Step>
              <Drawer.Step value="step3" title="Step 3" description="Description" />
            </Drawer.Steps>

            <Drawer.DualPaneMain>
              <Drawer.Header>
                <Drawer.Title>{currentStep}</Drawer.Title>
                <Drawer.Description>
                  Click any step or substep you've already visited — even ones ahead of where you are now — to
                  jump straight to it. Setting the rail's <code>value</code> to a substep automatically marks
                  the parent step as active.
                </Drawer.Description>
              </Drawer.Header>

              <Drawer.Body>
                <p>Active value: <strong>{currentStep}</strong></p>
              </Drawer.Body>
            </Drawer.DualPaneMain>
          </Drawer.DualPane>
        </Drawer.Content>
      </Drawer.Root>
    );

}`}
/>

```typescript jsx
const [currentStep, setCurrentStep] = useState('step2.2');

<Drawer.Steps value={currentStep} onValueChange={setCurrentStep} title="Title">
  <Drawer.Step value="step1" title="Step 1" description="Description" />

  {/* Drawer.Step accepts Drawer.SubStep children for sub-flows. */}
  <Drawer.Step value="step2" title="Step 2" description="Description">
    <Drawer.SubStep value="step2.1" title="Step 2.1" />
    <Drawer.SubStep value="step2.2" title="Step 2.2" />
    <Drawer.SubStep value="step2.3" title="Step 2.3" />
  </Drawer.Step>

  <Drawer.Step value="step3" title="Step 3" description="Description" />
</Drawer.Steps>
```

### Reference content

Use `Drawer.Rail` when the rail holds reference material that the user scans while they work in the main pane — a glossary, a definition list, a related-records list, a file tree, etc. The rail keeps the chrome (surface, border, optional title, independent scroll area) but doesn't enforce any list semantics, so you can render any markup you like inside.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [open, setOpen] = React.useState(false);

    return (
      <Drawer.Root open={open} onOpenChange={setOpen}>
        <Drawer.Trigger>
          <Button>Open dual pane drawer with rail</Button>
        </Drawer.Trigger>

        <Drawer.Content size="lg">
          <Drawer.DualPane>
            <Drawer.Rail title="Rail" aria-label="Rail">
              <div className="flex h-full items-center justify-center">
                <p className="font-heading-section text-cn-3">Content</p>
              </div>
            </Drawer.Rail>

            <Drawer.DualPaneMain>
              <Drawer.Header>
                <Drawer.Title>Title</Drawer.Title>
                <Drawer.Description>Description</Drawer.Description>
              </Drawer.Header>

              <Drawer.Body>
                <div className="flex h-full items-center justify-center">
                  <p className="font-heading-section text-cn-3">Content</p>
                </div>
              </Drawer.Body>

              <Drawer.Footer>
                <ButtonLayout.Root>
                  <ButtonLayout.Primary>
                    <Button>Create pipeline</Button>
                  </ButtonLayout.Primary>
                  <ButtonLayout.Secondary>
                    <Drawer.Close asChild>
                      <Button variant="outline">Cancel</Button>
                    </Drawer.Close>
                  </ButtonLayout.Secondary>
                </ButtonLayout.Root>
              </Drawer.Footer>
            </Drawer.DualPaneMain>
          </Drawer.DualPane>
        </Drawer.Content>
      </Drawer.Root>
    );

}`}
/>

```typescript jsx
<Drawer.Root open={open} onOpenChange={setOpen}>
  <Drawer.Trigger>
    <Button>Open dual pane drawer</Button>
  </Drawer.Trigger>

  <Drawer.Content size="lg">
    <Drawer.DualPane>
      <Drawer.Rail title="Rail" aria-label="Rail">
        <div className="flex h-full items-center justify-center">
          <p className="font-heading-section text-cn-3">Content</p>
        </div>
      </Drawer.Rail>

      <Drawer.DualPaneMain>
        <Drawer.Header>
          <Drawer.Title>Title</Drawer.Title>
          <Drawer.Description>Description</Drawer.Description>
        </Drawer.Header>

        <Drawer.Body>
          <div className="flex h-full items-center justify-center">
            <p className="font-heading-section text-cn-3">Content</p>
          </div>
        </Drawer.Body>

        <Drawer.Footer>
          <ButtonLayout.Root>
            <ButtonLayout.Primary>
              <Button>Create pipeline</Button>
            </ButtonLayout.Primary>
            <ButtonLayout.Secondary>
              <Drawer.Close asChild>
                <Button variant="outline">Cancel</Button>
              </Drawer.Close>
            </ButtonLayout.Secondary>
          </ButtonLayout.Root>
        </Drawer.Footer>
      </Drawer.DualPaneMain>
    </Drawer.DualPane>
  </Drawer.Content>
</Drawer.Root>
```

### Nested drawer

A dual pane drawer can host [nested drawers](#nested-drawers) the same way any other drawer can — drop a `Drawer.Root` anywhere inside the parent's content (typically inside `Drawer.DualPaneMain`, but the rail works too) and it automatically becomes a nested drawer. Use this when an action inside the dual pane needs its own focused workspace — for example creating a sub-resource referenced by the parent form, viewing a record's full detail, or confirming a destructive change — without losing the user's place in the parent flow.

While the nested drawer is open the parent dual pane stays mounted (its state, scroll position, and active step are preserved) and shifts back slightly to give the nested drawer visual prominence. Closing the nested drawer restores the parent.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [open, setOpen] = React.useState(false);
    const [step, setStep] = React.useState('details');

    return (
      <Drawer.Root open={open} onOpenChange={setOpen}>
        <Drawer.Trigger>
          <Button>Open dual pane drawer with nested drawer</Button>
        </Drawer.Trigger>

        <Drawer.Content size="lg">
          <Drawer.DualPane>
            <Drawer.Steps title="Title" value={step} onValueChange={setStep} aria-label="Steps">
              <Drawer.Step value="details" title="Step" description="Description" />
              <Drawer.Step value="trigger" title="Step" description="Description" />
              <Drawer.Step value="review" title="Step" description="Description" />
            </Drawer.Steps>

            <Drawer.DualPaneMain>
              <Drawer.Header>
                <Drawer.Title>Step Title</Drawer.Title>
                <Drawer.Description>Description</Drawer.Description>
              </Drawer.Header>

              <Drawer.Body>
                <Drawer.Root>
                  <Drawer.Trigger>
                    <Button variant="outline">Open another drawer</Button>
                  </Drawer.Trigger>

                  <Drawer.Content size="sm">
                    <Drawer.Header>
                      <Drawer.Title>Title</Drawer.Title>
                      <Drawer.Description>Description</Drawer.Description>
                    </Drawer.Header>

                    <Drawer.Body>
                      <div className="flex h-full items-center justify-center">
                        <p className="font-heading-section text-cn-3">Content</p>
                      </div>
                    </Drawer.Body>

                    <Drawer.Footer>
                      <ButtonLayout.Root>
                        <ButtonLayout.Primary>
                          <Drawer.Close asChild>
                            <Button>Create connector</Button>
                          </Drawer.Close>
                        </ButtonLayout.Primary>
                        <ButtonLayout.Secondary>
                          <Drawer.Close asChild>
                            <Button variant="outline">Cancel</Button>
                          </Drawer.Close>
                        </ButtonLayout.Secondary>
                      </ButtonLayout.Root>
                    </Drawer.Footer>
                  </Drawer.Content>
                </Drawer.Root>
              </Drawer.Body>

              <Drawer.Footer>
                <ButtonLayout.Root>
                  <ButtonLayout.Primary>
                    <Button onClick={() => setStep('trigger')}>Continue</Button>
                  </ButtonLayout.Primary>
                  <ButtonLayout.Secondary>
                    <Drawer.Close asChild>
                      <Button variant="outline">Cancel</Button>
                    </Drawer.Close>
                  </ButtonLayout.Secondary>
                </ButtonLayout.Root>
              </Drawer.Footer>
            </Drawer.DualPaneMain>
          </Drawer.DualPane>
        </Drawer.Content>
      </Drawer.Root>
    );

}`}
/>

```typescript jsx
<Drawer.Root open={open} onOpenChange={setOpen}>
  <Drawer.Trigger>
    <Button>Open dual pane drawer with nested drawer</Button>
  </Drawer.Trigger>

  <Drawer.Content size="lg">
    <Drawer.DualPane>
      <Drawer.Steps title="Title" value={step} onValueChange={setStep}>
        <Drawer.Step value="details" title="Step" description="Description" />
        <Drawer.Step value="trigger" title="Step" description="Description" />
        <Drawer.Step value="review" title="Step" description="Description" />
      </Drawer.Steps>

      <Drawer.DualPaneMain>
        <Drawer.Header>
          <Drawer.Title>Step Title</Drawer.Title>
          <Drawer.Description>Description</Drawer.Description>
        </Drawer.Header>

        <Drawer.Body>
          {/* Drawer.Root placed inside the parent's content automatically
              renders as a nested drawer (no `nested` prop required). */}
          <Drawer.Root>
            <Drawer.Trigger>
              <Button variant="outline">Open another drawer</Button>
            </Drawer.Trigger>

              <Drawer.Content size="sm">
                <Drawer.Header>
                  <Drawer.Title>Title</Drawer.Title>
                  <Drawer.Description>Description</Drawer.Description>
                </Drawer.Header>
                <Drawer.Body>
                  <div className="flex h-full items-center justify-center">
                    <p className="font-heading-section text-cn-3">Content</p>
                  </div>
                </Drawer.Body>
              <Drawer.Footer>
                <ButtonLayout.Root>
                  <ButtonLayout.Primary>
                    <Drawer.Close asChild>
                      <Button>Create connector</Button>
                    </Drawer.Close>
                  </ButtonLayout.Primary>
                  <ButtonLayout.Secondary>
                    <Drawer.Close asChild>
                      <Button variant="outline">Cancel</Button>
                    </Drawer.Close>
                  </ButtonLayout.Secondary>
                </ButtonLayout.Root>
              </Drawer.Footer>
            </Drawer.Content>
          </Drawer.Root>
        </Drawer.Body>

        <Drawer.Footer>{/* parent footer */}</Drawer.Footer>
      </Drawer.DualPaneMain>
    </Drawer.DualPane>
  </Drawer.Content>
</Drawer.Root>
```

### Sizes

The dual pane drawer inherits its width from `Drawer.Content`'s `size` prop. The panes always render side by side. The right pane is fluid — it shrinks with the available space — but it has a `min-width` of the `xs` drawer size (`var(--cn-drawer-xs)`) so it never collapses below a usable width. If the drawer becomes narrower than the steps rail plus that minimum (for example a `size="full"` drawer on a narrow viewport, a fixed `size` like `lg` on a viewport that's smaller than `lg`, or a small fixed `size`), the dual pane scrolls horizontally instead of clipping content. Use `md` or larger to give both panes comfortable room without scrolling.

`Drawer.Content` itself is capped at the viewport's width and height (`100vw` / `100vh`), so a fixed size never overflows the screen — when the viewport is narrower than the chosen `size`, the drawer fills the viewport and the dual pane behaves the same as `full`.

You can override the right pane's floor by setting `--cn-drawer-dual-pane-main-min-width` on `.cn-drawer-dual-pane`.

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const sizes = ['md', 'lg', 'xl', 'full'];
    const [openSize, setOpenSize] = React.useState(null);

    const steps = [
      { value: 'details', title: 'Step', description: 'Description' },
      { value: 'configuration', title: 'Step', description: 'Description' },
      { value: 'permissions', title: 'Step', description: 'Description' },
      { value: 'review', title: 'Step', description: 'Description' },
    ];

    const [currentStep, setCurrentStep] = React.useState('configuration');
    const currentIndex = steps.findIndex((s) => s.value === currentStep);
    const activeStep = steps[currentIndex] ?? steps[0];

    return (
      <ButtonLayout orientation="vertical">
        {sizes.map((size) => (
          <Drawer.Root
            key={size}
            open={openSize === size}
            onOpenChange={(next) => setOpenSize(next ? size : null)}
          >
            <Drawer.Trigger>
              <Button>Open dual pane drawer with '{size}' size</Button>
            </Drawer.Trigger>

            <Drawer.Content size={size}>
              <Drawer.DualPane>
                <Drawer.Steps
                  value={currentStep}
                  onValueChange={setCurrentStep}
                  title={\`Size: \${size}\`}
                >
                  {steps.map((step) => (
                    <Drawer.Step
                      key={step.value}
                      value={step.value}
                      title={step.title}
                      description={step.description}
                    />
                  ))}
                </Drawer.Steps>

                <Drawer.DualPaneMain>
                  <Drawer.Header>
                    <Drawer.Tagline>{currentIndex + 1} of {steps.length}</Drawer.Tagline>
                    <Drawer.Title>Step title</Drawer.Title>
                    <Drawer.Description>{activeStep.description}</Drawer.Description>
                  </Drawer.Header>

                  <Drawer.Body>
                    <p className="font-body-normal">
                      The steps rail and the main pane always render side by side. The main pane shrinks with the viewport but never below the <code>xs</code> drawer width — past that point the dual pane scrolls horizontally.
                    </p>
                  </Drawer.Body>

                  <Drawer.Footer>
                    <ButtonLayout.Root>
                      <ButtonLayout.Primary>
                        <Button>Continue</Button>
                      </ButtonLayout.Primary>
                      <ButtonLayout.Secondary>
                        <Drawer.Close asChild>
                          <Button variant="outline">Cancel</Button>
                        </Drawer.Close>
                      </ButtonLayout.Secondary>
                    </ButtonLayout.Root>
                  </Drawer.Footer>
                </Drawer.DualPaneMain>
              </Drawer.DualPane>
            </Drawer.Content>
          </Drawer.Root>
        ))}
      </ButtonLayout>
    );

}`}
/>

### `DualPane`

Dual-pane row layout wrapper.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description:
        "A rail (Drawer.Rail or Drawer.Steps) plus a Drawer.DualPaneMain.",
      required: true,
      value: "ReactNode",
    },
    {
      name: "className",
      description: "Class to apply to the dual pane layout wrapper.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
  ]}
/>

### `Rail`

Generic left rail for a dual pane drawer. Provides the rail's chrome — fixed width, surface, border, optional title header, and an independent scroll area — and renders arbitrary children. Use this for non-stepped content like a glossary, definition list, file tree, related-records list, or browse view. For numbered multi-step flows, use [`Drawer.Steps`](#steps) instead.

Renders an `<aside>` element so screen readers expose it as a complementary region; pair it with `aria-label` to give that region a name.

<DocsPage.PropsTable
  props={[
    {
      name: "title",
      description:
        "Optional heading rendered at the top of the rail. Stays pinned while the rail body scrolls underneath it.",
      required: false,
      value: "ReactNode",
      defaultValue: "undefined",
    },
    {
      name: "aria-label",
      description: "Accessible name for the complementary region.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "className",
      description: "Class to apply to the rail container.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "children",
      description: "Content rendered inside the rail's scrollable body.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Steps`

Left step rail with independent scrolling and controlled active step state.

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description: "Value of the active step.",
      required: true,
      value: "string",
    },
    {
      name: "onValueChange",
      description:
        "Callback when the user selects a completed or current step.",
      required: false,
      value: "(value: string) => void",
      defaultValue: "undefined",
    },
    {
      name: "title",
      description:
        "Optional heading rendered above the step list (for example the drawer's title).",
      required: false,
      value: "ReactNode",
      defaultValue: "undefined",
    },
    {
      name: "aria-label",
      description: "Accessible name for the step navigation region.",
      required: false,
      value: "string",
      defaultValue: "Drawer steps",
    },
    {
      name: "children",
      description: "Drawer.Step components.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Step`

A single step in the step rail. Renders an indicator circle with the step number for active and upcoming steps, and a check mark for completed steps. Active and completed steps are rendered as buttons; upcoming steps are read-only. Pass `Drawer.SubStep` children to give the step its own sub-flow — substeps appear under the step when it is active.

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description: "Unique value used to identify the step.",
      required: true,
      value: "string",
    },
    {
      name: "title",
      description: "Step title.",
      required: true,
      value: "ReactNode",
    },
    {
      name: "description",
      description: "Optional description rendered below the title.",
      required: false,
      value: "ReactNode",
      defaultValue: "undefined",
    },
    {
      name: "children",
      description:
        "Optional Drawer.SubStep children. Substeps render under the step when the step is active (when the rail's value matches the step or any of its substeps).",
      required: false,
      value: "ReactNode",
      defaultValue: "undefined",
    },
  ]}
/>

### `SubStep`

A single substep nested inside a `Drawer.Step`. Renders a smaller, no-circle indicator (check mark for completed, filled dot for active, dash for upcoming) plus a title. Completed and active substeps are rendered as buttons; upcoming substeps are read-only. The parent step is automatically treated as active whenever any of its substeps is the rail's `value`, and top-level steps after the parent stay upcoming until the user moves past the parent.

<DocsPage.PropsTable
  props={[
    {
      name: "value",
      description:
        "Unique value used to identify the substep. Setting Drawer.Steps' value to this marks the parent step as active.",
      required: true,
      value: "string",
    },
    {
      name: "title",
      description: "Substep title.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `DualPaneMain`

Right-hand column for the active step header, body, and footer.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Drawer.Header, Drawer.Body, and Drawer.Footer components.",
      required: true,
      value: "ReactNode",
    },
    {
      name: "className",
      description: "Class to apply to the main pane wrapper.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
  ]}
/>

## API Reference

### `Root`

Contains [`Drawer.Trigger`](#trigger) and [`Drawer.Content`](#content) components.

<DocsPage.PropsTable
  props={[
    {
      name: "defaultOpen",
      description:
        "The open state of the drawer when it is initially rendered. Use when you do not need to control its open state.",
      required: false,
      value: "boolean",
      defaultValue: "undefined",
    },
    {
      name: "open",
      description:
        "When set, operate in controlled mode in combination with onOpenChange.",
      required: false,
      value: "boolean",
      defaultValue: "undefined",
    },
    {
      name: "onOpenChange",
      description: "Callback notified when open state changes.",
      required: false,
      value: "(open: boolean) => void",
      defaultValue: "undefined",
    },
    {
      name: "modal",
      description:
        "The modality of the dialog. When set to true, interaction with outside elements will be disabled and only dialog content will be visible to screen readers.",
      required: false,
      value: "boolean",
      defaultValue: "true",
    },
    {
      name: "container",
      description: "Specify a container element to portal the drawer into.",
      required: false,
      value: "HTMLElement",
      defaultValue: "document.body",
    },
    {
      name: "direction",
      description: "Direction the drawer will originate from.",
      required: false,
      value: "'right' | 'left' | 'top' | 'bottom'",
      defaultValue: "right",
    },
    {
      name: "onAnimationEnd",
      description:
        "Gets triggered after the open or close animation ends, it receives an open argument with the open state of the drawer by the time the function was triggered.",
      required: false,
      value: "(open: boolean) => void",
      defaultValue: "undefined",
    },
    {
      name: "dismissible",
      description:
        "When false dragging, clicking outside, pressing esc, etc. will not close the drawer. Use this in combination with the open prop, otherwise you won't be able to open/close the drawer.",
      required: false,
      value: "boolean",
      defaultValue: "true",
    },
    {
      name: "handleOnly",
      description: "When true dragging will only be possible by the handle.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "repositionInputs",
      description:
        "When true will reposition inputs rather than scroll then into view if the keyboard is in the way. Setting it to false will fall back to the default browser behavior.",
      required: false,
      value: "boolean",
      defaultValue: "true",
    },
    {
      name: "maxStackDepth",
      description:
        "Maximum number of nested drawers that visually stack. Drawers beyond this limit still open but cover the deepest visible drawer instead of adding more offset.",
      required: false,
      value: "number",
      defaultValue: "7",
    },
    {
      name: "children",
      description: "Drawer.Trigger and Drawer.Content components.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Trigger`

Used to open a drawer. Essential for registering the trigger element with the dialog focus manager.
If a drawer is opened without the `Trigger` component (e.g., a dropdown item), focus will not return to the trigger element when the drawer is closed. To address this use `useCustomDialogTrigger` hook to register your custom trigger element.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Trigger element (only one child allowed)",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Close`

An optional button that closes the drawer.

<DocsPage.PropsTable
  props={[
    {
      name: "asChild",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>

### `Content`

Contains [`Drawer.Header`](#header), [`Drawer.Body`](#body) and [`Drawer.Footer`](#footer) components to be rendered in the open drawer.

<DocsPage.PropsTable
  props={[
    {
      name: "size",
      description: "Width | height of the drawer.",
      required: false,
      value: " '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl' | 'full'",
      defaultValue: "sm",
    },
    {
      name: "children",
      description: "Drawer.Header, Drawer.Body and Drawer.Footer components.",
      required: true,
      value: "ReactNode",
    },
    {
      name: "overlayClassName",
      description: "Class to apply to the overlay.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "forceWithOverlay",
      description:
        "When true, the drawer will always render an overlay even if the modal prop is false.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>

### `Header`

An optional container for the [`Drawer.Tagline`](#tagline), [`Drawer.Title`](#title) and [`Drawer.Description`](#description) components.

<DocsPage.PropsTable
  props={[
    {
      name: "icon",
      description: "Name of an icon to display (cannot be combined with logo).",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "hideClose",
      description: "When set, do not display the close 'x' button.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "logo",
      description: "Name of a logo to display (cannot be combined with icon).",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "children",
      description:
        "Drawer.Tagline, Drawer.Title and Drawer.Description components.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Tagline`

An optional tagline above the title.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "Content to display.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Title`

An optional accessible title to be announced when the drawer is opened.

<DocsPage.PropsTable
  props={[
    {
      name: "asChild",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>

### `Description`

An optional accessible description to be announced when the drawer is opened.

<DocsPage.PropsTable
  props={[
    {
      name: "asChild",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
  ]}
/>

### `Body`

A scrollable wrapper for all content that doesn’t belong to the [`Drawer.Header`](#header) or [`Drawer.Footer`](#footer).

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Class to apply to the viewport of the scrollable area.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "scrollAreaClassName",
      description: "Class to apply to the scrollable area.",
      required: false,
      value: "string",
      defaultValue: "undefined",
    },
    {
      name: "children",
      description: "Content to display, rendered as a ScrollArea.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>

### `Footer`

An optional footer wrapper.
