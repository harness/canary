---
title: TabNav
description: TabNav component
---

The `TabNav` component is used to create a tabbed navigation interface. It consists of a root container and individual tab items that can be linked to different routes.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<TabNav.Root>
  <TabNav.Item to="/home" label="Home" />
  <TabNav.Item to="/about" label="About" />
  <TabNav.Item to="/contact" label="Contact" />
</TabNav.Root>`}
/>

## Usage

```typescript jsx
import { TabNav } from '@harnessio/ui/components'

//...

return (
  <TabNav.Root>
    <TabNav.Item to="/home" label="Home" />
    <TabNav.Item to="/about" label="About" />
    <TabNav.Item to="/contact" label="Contact" />
  </TabNav.Root>
)
```

## Anatomy

```typescript jsx
<TabNav.Root>
  <TabNav.Item to="/home" label="Home" />
  <TabNav.Item to="/about" label="About" />
  <TabNav.Item to="/contact" label="Contact" />
</TabNav.Root>
```

## API Reference

### [`TabNav`](packages/ui/src/components/tabnav.tsx)

The [`TabNav`](packages/ui/src/components/tabnav.tsx) component provides a tabbed navigation interface. It consists of the following subcomponents:

### [`TabNav.Root`](packages/ui/src/components/tabnav.tsx)

The root container for the tab navigation. It wraps the individual tab items.

```typescript jsx
<TabNav.Root>
  {/* TabNav.Item elements as children */}
</TabNav.Root>
```

### [`TabNav.Item`](packages/ui/src/components/tabnav.tsx)

An individual tab item that links to a specific route. It accepts the following props:

```typescript jsx
<TabNav.Item
  to="/path"              // The route path to link to
  label="Tab Label"       // The label to display for the tab
  activeClassname="..."   // [OPTIONAL] Additional class names for the active state
/>
```

### Props

- `to`: A string representing the route path to link to.
- `label`: A string representing the label to display for the tab.
- `activeClassname`: An optional string for additional class names to apply when the tab is active.
