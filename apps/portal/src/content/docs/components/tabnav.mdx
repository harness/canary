---
title: TabNav
description: TabNav component documentation
---

The `TabNav` component provides a tabbed navigation interface. It consists of a root container and individual tab items that can be linked to different routes.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<TabNav.Root>
  <TabNav.Item to="/home">Home</TabNav.Item>
  <TabNav.Item to="/about">About</TabNav.Item>
  <TabNav.Item to="/contact">Contact</TabNav.Item>
</TabNav.Root>`}
/>

## Usage

```typescript jsx
import { TabNav } from '@harnessio/ui/components'

return (
  <TabNav.Root>
    <TabNav.Item to="/home">Home</TabNav.Item>
    <TabNav.Item to="/about">About</TabNav.Item>
    <TabNav.Item to="/contact">Contact</TabNav.Item>
  </TabNav.Root>
)
```

## Anatomy

The TabNav component can be used to create a tabbed navigation interface.

```typescript jsx
<TabNav.Root>
  <TabNav.Item to="/home">Home</TabNav.Item>
  <TabNav.Item to="/about">About</TabNav.Item>
  <TabNav.Item to="/contact">Contact</TabNav.Item>
</TabNav.Root>
```

## API Reference

### TabNav.Root

The `Root` component serves as the main container for the tab navigation.

<DocsPage.PropsTable
  props={[
    {
      name: "children",
      description: "The child components to render inside the root",
      required: true,
    },
  ]}
/>

### TabNav.Item

The `Item` component is used to display an individual tab item.

<DocsPage.PropsTable
  props={[
    {
      name: "to",
      description: "The route path to link to",
      required: true,
      value: "string",
    },
    {
      name: "children",
      description: "The label to display for the tab",
      required: true,
      value: "ReactNode",
    },
    {
      name: "activeClassName",
      description: "Additional class names for the active state",
      required: false,
      value: "string",
    },
  ]}
/>
