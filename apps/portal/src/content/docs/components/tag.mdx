---
title: Tag
description: Interactive tag component for displaying labels and actions
beta: true
---

The `Tag` component is used to display small, interactive elements with optional icons, reset functionality, and customizable styles.

import { DocsPage } from "@/components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-cn-2xl">
  
{/* Themes */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Themes
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    {[
      "gray",
      "blue",
      "brown",
      "cyan",
      "green",
      "indigo",
      "lime",
      "mint",
      "orange",
      "pink",
      "purple",
      "red",
      "violet",
      "yellow",
    ].map((theme) => (
      <Tag
        key={theme}
        value={theme.charAt(0).toUpperCase() + theme.slice(1)}
        theme={theme}
      />
    ))}
  </div>
</div>

{/* Variants */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Variants
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag value="Outline Tag" variant="outline" theme="blue" />
    <Tag value="Secondary Tag" variant="secondary" theme="green" />
  </div>
</div>

{/* Sizes */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Sizes
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag value="Default Size" theme="blue" />
    <Tag value="Small Size" size="sm" theme="blue" />
    <Tag value="Small Size" size="sm" theme="blue" icon="label" />
    <Tag
      value="Small Size"
      size="sm"
      theme="blue"
      icon="label"
      actionIcon="xmark"
      onActionClick={() => alert("Reset clicked")}
    />
  </div>
</div>

{/* Rounded Tags */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Rounded Tags
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag value="Rounded Outline Tag" variant="outline" rounded theme="pink" />
    <Tag
      value="Rounded Secondary Tag"
      variant="secondary"
      rounded
      theme="orange"
    />
  </div>
</div>

{/* Split Tags */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Split Tags
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag label="Split" value="Lime" variant="secondary" theme="lime" />
    <Tag label="Split" value="Pink" variant="secondary" theme="pink" />
    <Tag label="Split" value="Blue" variant="secondary" theme="blue" />
    <Tag
      label="Split"
      value="Reset"
      variant="secondary"
      theme="orange"
      actionIcon="xmark"
      onActionClick={() => alert("Reset clicked")}
    />
  </div>
</div>

{/* Tags with Icon */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Tags with Icon
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag value="Outline Icon" variant="outline" showIcon theme="yellow" />
    <Tag value="Secondary Icon" variant="secondary" showIcon theme="violet" />
    <Tag
      value="Rounded Outline Icon"
      variant="outline"
      rounded
      showIcon
      theme="brown"
    />
    <Tag
      value="Rounded Secondary Icon"
      variant="secondary"
      rounded
      showIcon
      theme="indigo"
    />
  </div>
</div>

{/* Tags with Custom Icon */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Tags with Custom Icon
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag
      value="Custom Icon"
      variant="outline"
      showIcon
      icon="plus"
      theme="mint"
    />
    <Tag
      value="Custom Icon Secondary"
      variant="secondary"
      showIcon
      icon="plus"
      theme="cyan"
    />
    <Tag
      value="Rounded Custom Icon"
      variant="outline"
      rounded
      showIcon
      icon="plus"
      theme="orange"
    />
    <Tag
      value="Rounded Custom Icon Secondary"
      variant="secondary"
      rounded
      showIcon
      icon="plus"
      theme="pink"
    />
  </div>
</div>

{/* Tags with Reset */}

<div>
  <Text variant="heading-subsection" as="h4" className="mb-cn-sm">
    Tags with Reset
  </Text>
  <div className="flex flex-wrap items-end gap-cn-sm">
    <Tag
      value="Outline Reset"
      variant="outline"
      actionIcon="xmark"
      onActionClick={() => alert("Reset clicked")}
      theme="blue"
    />
    <Tag
      value="Secondary Reset"
      variant="secondary"
      icon="label"
      actionIcon="xmark"
      onActionClick={() => alert("Reset clicked")}
      theme="green"
    />
    <Tag
      value="Rounded Outline Reset"
      variant="outline"
      rounded
      actionIcon="xmark"
      onActionClick={() => alert("Reset clicked")}
      theme="purple"
    />
    <Tag
      value="Rounded Secondary Reset"
      variant="secondary"
      rounded
      actionIcon="xmark"
      onActionClick={() => alert("Reset clicked")}
      theme="red"
    />
  </div>
</div>

{/* Tags with Icon and Reset */}

  <div>
    <Text variant="heading-subsection" as="h4" className="mb-cn-sm">Tags with Icon and Reset</Text>
    <div className="flex flex-wrap items-end gap-cn-sm">
      <Tag
        value="Outline Icon Reset"
        variant="outline"
        showIcon
        actionIcon="xmark"
        onActionClick={() => alert("Reset clicked")}
        theme="yellow"
      />
      <Tag
        value="Secondary Icon Reset"
        variant="secondary"
        showIcon
        actionIcon="xmark"
        onActionClick={() => alert("Reset clicked")}
        theme="violet"
      />
      <Tag
        value="Rounded Outline Icon Reset"
        variant="outline"
        rounded
        showIcon
        actionIcon="xmark"
        onActionClick={() => alert("Reset clicked")}
        theme="brown"
      />
      <Tag
        value="Rounded Secondary Icon Reset"
        variant="secondary"
        rounded
        showIcon
        onClick={() => alert("Tag clicked")}
        actionIcon="xmark"
        onActionClick={() => alert("Reset clicked")}
        theme="indigo"
      />
    </div>
  </div>
</div>`}
/>

## Usage

```typescript jsx
import { Tag } from '@harnessio/ui/components'

// Basic usage
<Tag value="Default Tag" />

// Theme
<Tag value="Blue Tag" theme="blue" />

// Variant
<Tag value="Outline Tag" variant="outline" theme="blue" />

// Rounded tag
<Tag
  value="Rounded Outline Tag"
  variant="outline"
  rounded
  theme="pink"
/>

// Split tag
<Tag
  label="Split"
  value="Lime"
  variant="secondary"
  theme="lime"
/>

// Tag with icon
<Tag
  value="Outline Icon"
  variant="outline"
  showIcon
  theme="yellow"
/>

// Tag with custom icon
<Tag
  value="Custom Icon"
  variant="outline"
  showIcon
  icon="plus"
  theme="mint"
/>

// Tag with reset
<Tag
  value="Outline Reset"
  variant="outline"
  actionIcon="xmark"
  onActionClick={() => alert('Reset clicked')}
  theme="blue"
/>

// Tag with icon and reset
<Tag
  value="Outline Icon Reset"
  variant="outline"
  showIcon
  actionIcon="xmark"
  onActionClick={() => alert('Reset clicked')}
  theme="yellow"
/>
```

## Props

<DocsPage.PropsTable
  props={[
    {
      name: "variant",
      description: "Visual style of the tag.",
      required: false,
      value: "'outline' | 'secondary'",
      defaultValue: "'outline'",
    },
    {
      name: "size",
      description: "Size of the tag.",
      required: false,
      value: "'sm' | 'md'",
      defaultValue: "'md'",
    },
    {
      name: "theme",
      description: "Color theme of the tag.",
      required: false,
      value: "'gray' | 'blue' | 'green' | 'red' | ...",
      defaultValue: "'gray'",
    },
    {
      name: "rounded",
      description: "Applies rounded corners to the tag.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "icon",
      description: "Specifies the name of the icon to display.",
      required: false,
      value: "string",
    },
    {
      name: "actionIcon",
      description: "If true, displays a reset (close) icon.",
      required: false,
      value: "IconV2Name",
      defaultValue: "false",
    },
    {
      name: "onActionClick",
      description:
        "Callback function triggered when the reset icon is clicked.",
      required: false,
      value: "() => void",
    },
    {
      name: "enableHover",
      description: "If true, enables hover effects on the tag.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "label",
      description: "Optional key name for split tags.",
      required: false,
      value: "string",
    },
    {
      name: "value",
      description: "The main text content of the tag.",
      required: true,
      value: "string",
    },
  ]}
/>
