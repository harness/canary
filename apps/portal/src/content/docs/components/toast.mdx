---
title: Toast
description: Toast component
beta: true
---

import { Code, Card, Aside } from "@astrojs/starlight/components";
import { DocsPage } from "../../../components/docs-page";

`toastV2` is a toast component that is used to display notifications to the user. It is handled by `ToasterV2` component, that is added in the root of the app.

To show a simple toast, use the `toastV2` function.

<DocsPage.ComponentExample
  client:only
  code={`
    <Button
      onClick={() =>
        toastV2({
          title: 'Toast title',
          description: Array.from({ length: Math.floor(Math.random() * 1000) + 1 })
            .map(() => 'a')
            .join(''),
          options: {
            duration: 3000,
            action: {
              label: 'Undo',
              onClick: () => {
                console.log('Undo')
              }
            }
          }
        })
      }
    >
      Show toast
    </Button>`}
/>

<Code
  code={`
    toastV2({
      title: 'Toast title',
      description: 'Toast description',
      options: {
      /**
       * If duration is not provided, 
       * the toast will be closed after 10 seconds by default.
       */
        duration: 5000,
        dismissible: true,
        action: {
          label: 'Undo',
          onClick: () => {
            console.log('Undo')
          }
        }
      }
    })
  `}
  lang="typescript"
/>

## Toast types

`toastV2` can be used to show different types of toasts.

<ol>
  <li>- Default toast (`toastV2`)</li>
  <li>- Success toast (`toastV2.success`)</li>
  <li>- Danger toast (`toastV2.danger`)</li>
  <li>- Info toast (`toastV2.info`)</li>
  <li>- Loading toast (`toastV2.loading`)</li>
  <li>- Promise toast (`toastV2.promise`)</li>
</ol>

<Card title="Points to Note">

<ol className='space-y-4'>
  <li>Maximum number of toasts that can be displayed at a time is 3.</li>
  <li>- If <code>duration</code> is not provided, the toast will be closed after <strong>10 seconds</strong> by default.</li>
  <li>- Close button in toast will be hidden only if <code>dismissible</code> is set to false in <code>options</code>. By default, it is set to true.</li>
  <li>- Action button can be added to toast by providing <code>action</code> in <code>options</code>.</li>
  <li>- Clicking of action button will close the toast and execute the callback function provided in <code>action</code>.</li>
  <li>- Promise toast and Loading toast cannot be dismissed by the user, it will be closed after the promise is resolved or rejected (Promise toast) or when the work in progress task is completed (Loading toast).</li>
  <li>- Promise toast and Loading toast cannot display a <code>description</code></li>
</ol>
</Card>

## Usage

### Success toast

<DocsPage.ComponentExample
  client:only
  code={`
    <Button
      onClick={() =>
        toastV2.success({
          title: 'Toast title',
          description: "Lorem ipsum dolor sit amet, consectetur adipisicing elit. Magni magnam quibusdam fugit saepe voluptates aperiam perspiciatis ipsam! Ad cupiditate, tempora aspernatur a, veritatis, rem maxime est aliquam natus iure vero. Lorem ipsum dolor sit amet, consectetur adipisicing elit. Dignissimos quis reprehenderit quas! Praesentium, delectus perferendis? Dicta, reiciendis? Sint ipsa necessitatibus natus, magni, delectus consequuntur, aperiam asperiores in quasi commodi doloribus?",
          options: {
            duration: 5000,
            action: {
              label: 'Undo',
              onClick: () => {
                console.log('Undo')
              }
            }
          }
        })
      }
    >
      Show success toast
    </Button>`}
/>

### Danger toast

<DocsPage.ComponentExample
  client:only
  code={`
    <Button
      onClick={() =>
        toastV2.danger({
          title: 'Toast title',
          description: "Lorem ipsum dolor sit amet, consectetur adipisicing elit. Magni magnam quibusdam fugit saepe voluptates aperiam perspiciatis ipsam! Ad cupiditate, tempora aspernatur a, veritatis, rem maxime est aliquam natus iure vero.",
          options: {
            duration: 5000,
            action: {
              label: 'Undo',
              onClick: () => {
                console.log('Undo')
              }
            }
          }
        })
      }
    >
      Show danger toast
    </Button>`}
/>

### Info toast

<DocsPage.ComponentExample
  client:only
  code={`
    <Button
      onClick={() =>
        toastV2.info({
          title: 'Toast title',
          description: "Lorem ipsum dolor sit amet, consectetur adipisicing elit. Magni magnam quibusdam fugit saepe voluptates aperiam perspiciatis ipsam! Ad cupiditate, tempora aspernatur a, veritatis, rem maxime est aliquam natus iure vero.",
          options: {
            duration: 5000,
          }
        })
      }
    >
      Show info toast
    </Button>`}
/>

### Loading toast

<Aside type="caution">
  Loading toast needs to be closed manually. use `toastV2.dismiss` to close the
  toast using the toast id.
</Aside>

<DocsPage.ComponentExample
  client:only
  code={`
    <Button
      onClick={() => {
        const _toastId = toastV2.loading({
          title: 'Loading data...',
          options: {
            action: {
              label: 'Close',
              onClick: () => {
                toastV2.dismiss(_toastId)
              }
            }
          }
        })
      }}
    >
      Show loading toast
    </Button>
        `}
/>

### Promise toast

Promise toast accepts a **promise** as its **first argument** and an **options object** as its **second argument**.

<Aside type="caution">
  Promise toast cannot be dismissed by the user, it will be closed after 7
  seconds when the promise is resolved or rejected.
</Aside>

<DocsPage.ComponentExample
  client:only
  code={`
() => {
  return <Button
    onClick={() =>
      toastV2.promise(new Promise((resolve, reject) => {
      const random = Math.random()
      setTimeout(() => {
        if (random > 0.5) {
          resolve('Promise resolved')
        } else {
          reject('Promise rejected')
        }
      }, 5000)
    }), {
        loadingMessage: 'Promise in progress',
        successMessage: 'Promise resolved successfully',
        errorMessage: 'Promise rejected',
        options: {
          action: {
            label: 'Action',
            onClick: () => {
              console.log('Action')
            },
          },
        },
      })
    }

>

    Show promise toast

  </Button>
}
`}
/>

### Toast params

Toast params are the parameters that are passed to the toast function.

<DocsPage.PropsTable
  props={[
    {
      name: "title",
      description: "Title of the toast",
      required: true,
      value: "string",
    },
    {
      name: "options",
      description: "Options object for the toast",
      required: false,
      value: "ToastOptions",
    },
    {
      name: "description",
      description: "Description of the toast",
      required: false,
      value: "ReactNode",
    },
    {
      name: "loadingMessage",
      description:
        "It is specific for promise toast. Loading message to be displayed when the promise is in progress",
      required: true,
      value: "string",
    },
    {
      name: "successMessage",
      description:
        "It is specific for promise toast. Success message to be displayed when the promise is resolved",
      required: false,
      value: "string",
    },
    {
      name: "errorMessage",
      description:
        "It is specific for promise toast. Error message to be displayed when the promise is rejected",
      required: false,
      value: "string",
    },
  ]}
/>

### Toast options

Toast options are the configuration object that are passed to the toast parameter.

<DocsPage.PropsTable
  props={[
    {
      name: "duration",
      description:
        "Duration of the toast to be closed after. Value should be in milliseconds.",
      required: false,
      value: "number",
    },
    {
      name: "action",
      description: "Action button to be added to the toast",
      required: false,
      value: "Action",
    },
    {
      name: "dismissible",
      description: "Whether the toast can be dismissed by the user",
      required: false,
      value: "boolean",
    },
  ]}
/>
