import { IFormDefinition } from '@harnessio/forms'

import { InputDefinition } from '../form-inputs/factory/factory'

export const GROUP_DESCRIPTION = 'Group description.'

export const groupFormDefinition: IFormDefinition<InputDefinition> = {
  inputs: []
}
